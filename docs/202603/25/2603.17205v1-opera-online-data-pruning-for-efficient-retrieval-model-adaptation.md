---
title: "OPERA: Online Data Pruning for Efficient Retrieval Model Adaptation"
title_zh: OPERA：面向高效检索模型适配的在线数据剪枝
authors: "Haoyang Fang, Shuai Zhang, Yifei Ma, Hengyi Wang, Cuixiong Hu, Katrin Kirchhoff, Bernie Wang, George Karypis"
date: 2026-03-17
pdf: "https://arxiv.org/pdf/2603.17205v1"
tags: ["query:sr"]
score: 8.0
evidence: 用于高效检索模型适配的数据剪枝框架
tldr: "针对稠密检索模型在特定领域微调时训练数据质量不一的问题，本文提出OPERA框架。研究发现静态剪枝虽能提升排序性能但会损害召回率，为此作者提出两阶段动态剪枝（DP）策略，在训练过程中自适应调节查询和文档级的采样概率。实验证明，DP在提升检索和排序性能的同时，将训练时间缩短了50%以上，且在LLM检索器上表现出良好的通用性。"
selection_source: fresh_fetch
motivation: 旨在解决稠密检索模型微调中不同训练样本贡献不均，以及静态剪枝导致的质量与覆盖度权衡问题。
method: 提出一种两阶段动态剪枝策略，通过在训练过程中自适应调节查询和文档层面的采样概率来优先处理高质量样本。
result: "动态剪枝在NDCG@10和Recall@20上分别提升了1.9%和0.7%，且仅需不到50%的训练时间即可达到同等性能。"
conclusion: OPERA框架通过在线数据剪枝实现了检索模型适配的效率与效果双赢，且适用于包括大模型在内的多种架构。
---

## 摘要
领域特定微调对于稠密检索器（dense retrievers）至关重要，但并非所有训练对对学习过程的贡献都相同。我们提出了 OPERA，这是一个利用这种异质性来提高检索模型适配有效性和效率的数据剪枝框架。我们首先研究了静态剪枝（SP），它仅保留高相似度的查询-文档对，揭示了内在的质量-覆盖率权衡：排序性能（NDCG）有所提升，但由于查询多样性降低，检索性能（Recall）可能会下降。为了解决这一权衡，我们提出了一种两阶段动态剪枝（DP）策略，该策略在整个训练过程中自适应地调节查询和文档层面的采样概率，在优先考虑高质量样本的同时保持对完整训练集的访问。在涵盖六个领域的八个数据集上的评估证明了这两种方法的有效性：SP 相比标准微调提升了排序性能（NDCG@10 +0.5%），而 DP 在排序（NDCG@10 +1.9%）和检索（Recall@20 +0.7%）方面均取得了最强性能，在所有方法中的平均排名为 1.38。这些发现可以扩展到基于大语言模型（LLM）的稠密检索器 Qwen3-Embedding，证实了其与架构无关的优势。值得注意的是，DP 在不到标准微调所需训练时间 50% 的情况下，即可达到相当的性能。

## Abstract
Domain-specific finetuning is essential for dense retrievers, yet not all training pairs contribute equally to the learning process. We introduce OPERA, a data pruning framework that exploits this heterogeneity to improve both the effectiveness and efficiency of retrieval model adaptation. We first investigate static pruning (SP), which retains only high-similarity query-document pairs, revealing an intrinsic quality-coverage tradeoff: ranking (NDCG) improves while retrieval (Recall) can degrade due to reduced query diversity. To resolve this tradeoff, we propose a two-stage dynamic pruning (DP) strategy that adaptively modulates sampling probabilities at both query and document levels throughout training, prioritizing high-quality examples while maintaining access to the full training set. Evaluations across eight datasets spanning six domains demonstrate the effectiveness of both approaches: SP improves ranking over standard finetuning (NDCG@10 +0.5\%), while DP achieves the strongest performance on both ranking (NDCG@10 +1.9\%) and retrieval (Recall@20 +0.7\%), with an average rank of 1.38 across all methods. These findings scale to Qwen3-Embedding, an LLM-based dense retriever, confirming architecture-agnostic benefits. Notably, DP reaches comparable performance in less than 50\% of the training time required by standard finetuning.

---

## 论文详细总结（自动生成）

### 论文总结：OPERA —— 面向高效检索模型适配的在线数据剪枝

#### 1. 核心问题与研究动机
*   **核心问题**：在对稠密检索器（Dense Retrievers）进行领域特定微调时，训练数据中并非所有查询-文档对（Query-Document pairs）都具有同等价值。
*   **研究动机**：
    *   **效率需求**：全量数据微调耗时耗力。
    *   **质量差异**：训练集中存在噪声或低质量样本，影响模型性能。
    *   **现有局限**：传统的分类/生成任务剪枝方法将样本视为独立同分布（IID），忽略了检索任务特有的“查询-文档”两阶段采样结构。
    *   **质量-覆盖率权衡**：简单的静态剪枝（SP）虽然能提升排序指标（NDCG），但会因为剔除过多查询而降低检索覆盖率（Recall）。

#### 2. 方法论
OPERA 框架包含两种主要策略，核心思想是利用预训练模型的相似度得分作为质量指标：
*   **静态剪枝 (Static Pruning, SP)**：
    *   在训练前计算所有正向对的余弦相似度，仅保留得分最高的 $k$ 比例数据。
    *   **特点**：简单高效，显著提升排序性能，但存在召回率下降的风险。
*   **动态剪枝 (Dynamic Pruning, DP)**：
    *   **核心思想**：不直接丢弃数据，而是通过“软采样”调节概率。高质量样本被高频采样，低质量样本保留较低但非零的采样概率。
    *   **层级化设计**：在“查询级”和“文档级”两个粒度上分别进行剪枝，匹配检索训练的层级结构。
    *   **动态调度**：使用余弦退火调度（Cosine Scheduling）随训练进程调整采样强度（$\alpha, \beta$）和筛选比例（$r, v$），使模型在训练后期更聚焦于难例或高质量样本。
    *   **更新优化**：引入更新间隔（$I_u$），减少频繁计算得分带来的计算开销。
*   **理论支撑**：论文通过数学证明（Theorem 1）指出，只要评分函数识别真阳性样本的准确率高于数据集基础比例，剪枝就能改善查询表示。

#### 3. 实验设计
*   **数据集**：涵盖 6 个领域的 8 个数据集，包括营养（NFCorpus）、医疗（TripClick）、金融（FiQA）、非事实类 QA（ANTIQUE）、事实类 QA（TriviaQA, HotpotQA）和事实核查（FEVER）。
*   **模型架构**：
    *   **Encoder-only**：BGE-large-en-v1.5 (335M)。
    *   **Decoder-based (LLM)**：Qwen3-Embedding-0.6B。
*   **对比方法 (Baselines)**：
    *   预训练模型（Zero-shot）。
    *   标准全量微调（Standard FT）。
    *   InfoBatch（通用的动态剪枝方法）。
    *   随机剪枝（Random Pruning）。

#### 4. 资源与算力
*   **算力说明**：论文**未明确说明具体的 GPU 型号和数量**。
*   **训练细节**：
    *   BGE 模型训练 8,000 到 32,000 个 iteration，Batch Size 为 64。
    *   Qwen3 模型训练 2,000 个 iteration。
    *   使用了 FP16（BGE）和 BF16（Qwen3）精度训练。
*   **开销优化**：通过设置查询更新间隔，DP 的额外计算开销从 4.5% 降低至 1.64%。

#### 5. 实验数量与充分性
*   **实验规模**：在 8 个数据集上进行了详尽测试，涵盖了预训练阶段“见过”和“没见过”的数据。
*   **消融实验**：针对层级化剪枝（仅查询 vs 仅文档 vs 两者结合）、更新间隔、静态剪枝保留率等进行了消融分析。
*   **鲁棒性测试**：专门设计了带有标签噪声的去噪实验（Denoising Capability）。
*   **评价**：实验设计较为充分且客观，通过跨架构（BGE 和 Qwen3）的验证增强了结论的说服力。

#### 6. 主要结论与发现
*   **性能提升**：动态剪枝（DP）在所有方法中表现最优，NDCG@10 平均提升 1.9%，Recall@20 提升 0.7%。
*   **效率倍增**：DP 仅需不到 50% 的训练时间即可达到与标准微调相当的性能。
*   **权衡解决**：DP 成功解决了 SP 存在的“质量-覆盖率”权衡问题，实现了排序和检索指标的双赢。
*   **去噪能力**：在含有噪声的数据集上，SP 和 DP 的组合策略表现出极强的鲁棒性，优于全量微调。

#### 7. 优点
*   **检索专用设计**：首次针对检索任务的层级采样结构设计剪枝算法，而非生搬硬套分类任务的方法。
*   **架构无关性**：在传统双编码器和新型 LLM 嵌入模型上均有效。
*   **理论与实践结合**：提供了严谨的理论证明，并给出了可直接落地的工程优化方案（如更新间隔控制）。

#### 8. 不足与局限
*   **语言覆盖**：实验仅限于英语数据集，未验证在多语言环境下的表现。
*   **模型规模限制**：虽然测试了 LLM 检索器，但模型参数量较小（0.6B），未在更大规模（如 7B 或 70B）的模型上验证。
*   **超参数敏感性**：动态剪枝引入了多个超参数（如起始/结束强度、比例等），论文未详细讨论这些参数在不同场景下的敏感度或自动调优方法。
*   **随机性说明**：未报告多次运行的方差，难以评估结果在随机种子影响下的稳定性。

（完）
