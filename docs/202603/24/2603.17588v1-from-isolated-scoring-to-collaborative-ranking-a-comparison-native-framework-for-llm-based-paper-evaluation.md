---
title: "From Isolated Scoring to Collaborative Ranking: A Comparison-Native Framework for LLM-Based Paper Evaluation"
title_zh: 从独立评分到协同排序：一种基于大语言模型的原生比较式论文评估框架
authors: "Pujun Zheng, Jiacheng Yao, Jinquan Zheng, Chenyang Gu, Guoxiu He, Jiawei Liu, Yong Huang, Tianrui Guo, Wei Lu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17588v1"
tags: ["query:sr"]
score: 9.0
evidence: 基于大语言模型论文评估的协作排序框架
tldr: 针对大语言模型在论文评估中因采用绝对评分而导致泛化性差的问题，本文提出CNPE框架，将评估范式从孤立评分转向协作排序。该框架通过图相似度算法构建高质量论文对，并结合监督微调与比较奖励的强化学习，实现了更稳健的学术判断力，在多个数据集上显著提升了评估性能和泛化能力。
selection_source: fresh_fetch
motivation: 传统的绝对评分法受限于不同会议和时期的评分标准差异，导致模型容易拟合特定规则而缺乏稳健的学术判断力。
method: 提出CNPE框架，利用图相似度算法采样具有辨别力的论文对，并通过监督微调和基于比较奖励的强化学习进行优化。
result: "实验结果显示该框架比强基线DeepReview-14B平均提升了21.8%，并在五个未见数据集上表现出优异的泛化性能。"
conclusion: 将论文评估从孤立评分转变为比较排序能有效提升大语言模型的评价质量，是实现自动化论文评审的更优路径。
---

## 摘要
目前，大语言模型（LLMs）通过独立地为每篇论文分配绝对分数来应用于科学论文评估。然而，由于评分标准在不同会议、时期和评估准则之间存在差异，基于绝对分数训练的模型往往容易拟合狭隘的、特定背景的规则，而难以形成稳健的学术判断。为了克服这一局限性，我们建议将论文评估从独立评分转向协同排序。具体而言，我们设计了论文评估的原生比较框架（CNPE），将比较机制整合到数据构建和模型学习中。我们首先提出了一种基于图的相似度排序算法，以便从集合中采样更具信息量和辨别力的论文对。随后，我们通过监督微调和基于比较奖励的强化学习来增强相对质量判断。在推理阶段，模型对采样的论文对进行两两比较，并将这些偏好信号聚合为全局相对质量排序。实验结果表明，我们的框架在强基准模型 DeepReview-14B 的基础上实现了 21.8% 的平均相对提升，并对五个此前未见的数据集表现出稳健的泛化能力。

## Abstract
Large language models (LLMs) are currently applied to scientific paper evaluation by assigning an absolute score to each paper independently. However, since score scales vary across conferences, time periods, and evaluation criteria, models trained on absolute scores are prone to fitting narrow, context-specific rules rather than developing robust scholarly judgment. To overcome this limitation, we propose shifting paper evaluation from isolated scoring to collaborative ranking. In particular, we design \textbf{C}omparison-\textbf{N}ative framework for \textbf{P}aper \textbf{E}valuation (\textbf{CNPE}), integrating comparison into both data construction and model learning. We first propose a graph-based similarity ranking algorithm to facilitate the sampling of more informative and discriminative paper pairs from a collection. We then enhance relative quality judgment through supervised fine-tuning and reinforcement learning with comparison-based rewards. At inference, the model performs pairwise comparisons over sampled paper pairs and aggregates these preference signals into a global relative quality ranking. Experimental results demonstrate that our framework achieves an average relative improvement of \textbf{21.8\%} over the strong baseline DeepReview-14B, while exhibiting robust generalization to five previously unseen datasets. \href{https://github.com/ECNU-Text-Computing/ComparisonReview}{Code}.

---

## 论文详细总结（自动生成）

### 论文总结：从独立评分到协同排序（CNPE 框架）

#### 1. 论文的核心问题与整体含义（研究动机和背景）
论文指出，现有的基于大语言模型（LLM）的论文评估方法大多采用**独立评分（Isolated Scoring）**范式，即模型独立地为每篇论文分配一个绝对分数。这种方式存在显著缺陷：
*   **标准不一致性**：不同会议、不同时期、不同准则下的评分量表差异巨大，导致模型容易拟合特定背景的“死规则”，而非习得稳健的学术判断力。
*   **推理难度高**：让模型直接进行复杂的定量推理（打分）容易引发幻觉和检索偏差。
*   **泛化性差**：在特定数据集上训练的打分模型难以直接迁移到新的论文集合中。

**核心动机**：将论文评估从“孤立打分”转向“**协同排序（Collaborative Ranking）**”，通过比较两篇论文的相对质量来模拟人类评审的逻辑，从而提升评估的准确性、稳健性和跨领域泛化能力。

#### 2. 论文提出的方法论：CNPE 框架
论文提出了 **CNPE (Comparison-Native framework for Paper Evaluation)**，其核心思想是将“比较”逻辑贯穿于数据构建、模型训练和推理全过程。

*   **关键技术细节**：
    1.  **图增强的对采样算法 (GBR-BR)**：为了避免随机采样导致的低信息量对比，提出基于图的相似度排序算法。通过双向检索构建语义相似度图，优先采样研究领域重叠的论文对（领域内对比）和随机论文对（跨领域对比）。
    2.  **两阶段模型训练**：
        *   **冷启动监督微调 (SFT)**：利用高性能模型生成推理链，让模型学习如何分析论文的创新性、重要性和清晰度。
        *   **基于比较奖励的强化学习 (RLVR)**：采用改进的 GRPO 算法，设计了一种**可验证的比较奖励机制**。奖励不基于绝对分数，而是基于模型是否正确判断了两篇论文的相对优劣（以人类评审的平均分差为基准）。
    3.  **推理与偏好聚合**：在推理阶段，模型对采样的论文对进行两两比较，生成偏好信号。随后利用 **Bradley-Terry 模型**将这些局部偏好聚合为全局相对质量排序，并据此做出接受/拒绝的决策。

#### 3. 实验设计
*   **数据集**：
    *   **主实验**：ICLR-2025 会议数据（包含论文元数据及人类评审分数）。
    *   **泛化性实验**：ICML 2025、NeurIPS 2025、ACL 2025、EMNLP 2025、NAACL 2025。
*   **Benchmark 与对比方法**：
    *   **智能体类**：AIScientist, AgentReview（分别搭载 GPT, Gemini, GLM 等底座）。
    *   **训练类**：SEA-E, CycleReviewer-8B, DeepReview (7B/14B)。
    *   **比较类**：NAIP, PairReview。
*   **评估指标**：
    *   **决策指标**：Accuracy, F1, AUC, Cohen’s κ。
    *   **排序指标**：Spearman’s ρ, Pairwise Accuracy, MAP@20, NDCG@20。

#### 4. 资源与算力
*   **硬件环境**：使用了 **2 台 NVIDIA RTX 6000 Pro GPU**（每台 96GB 显存）。
*   **模型配置**：以 **Qwen2.5-7B-Instruct** 为底座，使用 **LoRA** 技术（Rank=16）进行高效微调。
*   **训练时长**：SFT 训练 1 个 epoch，强化学习（RL）训练 1 个 epoch。
*   **效率优势**：由于仅使用标题和摘要，且推理链较短，其计算成本仅为强基线 DeepReview 的 7.4%。

#### 5. 实验数量与充分性
*   **实验规模**：
    *   在 ICLR-2025 上进行了全面的基准测试。
    *   在 5 个全新的顶级会议数据集上验证了泛化性。
    *   进行了详尽的**消融实验**，验证了 SFT、RL、相似度采样、随机采样以及不同聚合算法（如 PageRank, HITS 等）的有效性。
    *   进行了**超参数分析**，探讨了样本数量 $n$ 和采样比例 $\alpha$ 对性能的影响。
*   **充分性评价**：实验设计非常充分且客观。通过跨模型底座对比、跨会议验证以及统计学检验（如 Mann-Whitney U 检验），证明了结果的可靠性。同时，论文还专门讨论了位置偏差（Positional Bias）的缓解情况。

#### 6. 论文的主要结论与发现
*   **性能领先**：CNPE-7B 在所有指标上均优于所有基线模型。相比 14B 规模的强基线 DeepReview，平均性能提升了 **21.8%**。
*   **排序能力极强**：在 MAP@20 指标上，CNPE 比第二名提升了 52.6%，证明了原生比较框架在识别高质量论文方面的巨大优势。
*   **泛化能力稳健**：模型在未见过的 2025 年各大顶会数据集上均能显著区分“接受”与“拒绝”的论文，甚至能捕捉到 Long Paper 与 Findings 之间细微的质量差异。
*   **参数效率**：仅用 7B 参数的模型通过合理的框架设计，击败了更大规模（14B+）或调用闭源 API 的复杂系统。

#### 7. 优点：方法或实验设计上的亮点
*   **范式创新**：成功将论文评估从不稳定的“绝对打分”转变为更符合人类认知逻辑的“相对比较”。
*   **数据高效**：仅利用标题和摘要（Metadata）即可达到极高精度，极大降低了处理长文本的计算开销和隐私风险。
*   **训练策略科学**：结合了冷启动 SFT 和基于规则触发的强化学习（RLVR），有效解决了 LLM 在学术评价中的位置偏差和逻辑一致性问题。
*   **实用性强**：提出的 GBR-BR 采样算法解决了大规模论文集两两对比导致的计算爆炸问题（$O(n^2)$ 降至可控范围）。

#### 8. 不足与局限
*   **领域局限**：实验主要集中在计算机科学（CS）领域的顶会，其他学科（如生物、医学、人文）的适用性有待验证。
*   **信息缺失**：仅依赖标题和摘要虽然高效，但不可避免地会丢失论文中的技术细节、实验严谨性和证明过程，可能导致对某些深奥贡献的误判。
*   **模型规模限制**：受限于算力，仅在 7B 模型上进行了实验，尚未探索更大规模模型（如 70B+）在该框架下的潜力。
*   **人机差距**：尽管超越了现有 AI 基线，但与资深人类评审的综合判断力相比，仍存在一定差距。

（完）
