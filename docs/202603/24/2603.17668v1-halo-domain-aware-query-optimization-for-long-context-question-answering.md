---
title: "Halo: Domain-Aware Query Optimization for Long-Context Question Answering"
title_zh: Halo：面向长上下文问答的领域感知查询优化
authors: "Pramod Chunduri, Francisco Romero, Ali Payani, Kexin Rong, Joy Arulraj"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17668v1"
tags: ["query:sr"]
score: 8.0
evidence: 针对科学研究和长文本问答的领域感知查询优化
tldr: 针对长文本问答中大模型处理效率低、成本高且难以有效利用领域知识的问题，本文提出了 Halo 框架。Halo 能够自动从用户提示词中提取领域知识，并将其转化为可执行算子，应用于文档剪枝、块过滤和答案排序等多个阶段。该框架不仅提升了回答准确率，还显著降低了计算成本，使开源模型能以极低成本达到顶级模型的性能水平。
selection_source: fresh_fetch
motivation: 现有的长文本问答系统无法系统地利用用户提供的领域知识来优化查询处理，导致在处理海量文档时准确性受限且成本高昂。
method: Halo 框架通过将领域知识转化为可执行算子，在查询执行流水线的不同阶段实施文档剪枝、无关块过滤和候选答案排序，并辅以运行时回退机制。
result: "实验表明，Halo 在多个领域数据集上比基准方法准确率提升高达 13%，成本降低 4.8 倍，并让开源模型以 78 倍的低成本接近顶级模型的表现。"
conclusion: Halo 证明了通过系统化地将领域知识融入查询优化流程，可以同时实现长文本问答的高精度与高经济性。
---

## 摘要
针对长文档的长上下文问答（QA）对于金融分析、法律审查和科学研究等应用至关重要。当前的方法，如通过单次大语言模型（LLM）调用处理整个文档或通过检索增强生成（RAG）检索相关分块，存在两个缺点：首先，随着上下文规模的增加，回答质量可能会下降，从而影响准确性；其次，迭代处理数百个输入文档可能会产生极高的 API 调用成本。为了提高回答质量并减少获得所需回答所需的迭代次数，用户倾向于在提示词中添加领域知识。然而，现有系统未能系统地捕获并利用这些知识来指导查询处理。领域知识被视为与文档并列的提示词标记：LLM 可能会也可能不会遵循它，计算成本没有降低，且当输出错误时，用户必须手动进行迭代。我们提出了 Halo，这是一个长上下文问答框架，它能自动从用户提示词中提取领域知识，并将其作为可执行算子应用于多阶段查询执行流水线中。Halo 识别了三种常见的领域知识形式——在文档的何处查找、忽略哪些内容以及如何验证答案——并将每种形式应用于最有效的流水线阶段：在分块选择前剪枝文档、在推理前过滤无关分块，以及在生成后对候选回答进行排序。为了处理不精确或无效的领域知识，Halo 包含一种回退机制，可在运行时检测低质量算子并选择性地禁用它们。我们在金融、文学和科学数据集上的评估表明，与基准方法相比，Halo 的准确率提高了高达 13%，成本降低了 4.8 倍，并使轻量级开源模型能够以 78 倍低的成本接近前沿 LLM 的准确率。

## Abstract
Long-context question answering (QA) over lengthy documents is critical for applications such as financial analysis, legal review, and scientific research. Current approaches, such as processing entire documents via a single LLM call or retrieving relevant chunks via RAG have two drawbacks: First, as context size increases, response quality can degrade, impacting accuracy. Second, iteratively processing hundreds of input documents can incur prohibitively high costs in API calls. To improve response quality and reduce the number of iterations needed to get the desired response, users tend to add domain knowledge to their prompts. However, existing systems fail to systematically capture and use this knowledge to guide query processing. Domain knowledge is treated as prompt tokens alongside the document: the LLM may or may not follow it, there is no reduction in computational cost, and when outputs are incorrect, users must manually iterate.   We present Halo, a long-context QA framework that automatically extracts domain knowledge from user prompts and applies it as executable operators across a multi-stage query execution pipeline. Halo identifies three common forms of domain knowledge - where in the document to look, what content to ignore, and how to verify the answer - and applies each at the pipeline stage where it is most effective: pruning the document before chunk selection, filtering irrelevant chunks before inference, and ranking candidate responses after generation. To handle imprecise or invalid domain knowledge, Halo includes a fallback mechanism that detects low-quality operators at runtime and selectively disables them. Our evaluation across finance, literature, and scientific datasets shows that Halo achieves up to 13% higher accuracy and 4.8x lower cost compared to baselines, and enables a lightweight open-source model to approach frontier LLM accuracy at 78x lower cost.

---

## 论文详细总结（自动生成）

### 论文总结：Halo —— 面向长上下文问答的领域感知查询优化

#### 1. 核心问题与整体含义
随着大语言模型（LLM）上下文窗口的扩大，处理长文档（如金融年报、法律合同、科研论文）的需求日益增长。然而，现有的长文本问答（QA）方法存在两大痛点：
*   **准确性瓶颈**：长上下文会导致模型出现“迷失在中间”（Lost in the Middle）现象，产生幻觉或遗漏关键信息。
*   **成本高昂**：直接调用前沿 LLM 处理数十万 Token 的文档成本极高，且 RAG（检索增强生成）常因检索分块破碎导致语义不连贯。
*   **领域知识利用不足**：用户常在 Prompt 中加入领域知识（如“只关注表格”、“忽略法律免责声明”），但现有系统仅将其视为普通 Token，无法将其转化为系统化的执行优化策略。

**Halo** 的核心动机是：**自动从用户 Prompt 中提取领域知识，并将其转化为可执行的数据库式算子，在查询执行的各个阶段主动优化路径，从而同时提升准确率并降低成本。**

---

#### 2. 方法论：核心思想与技术细节
Halo 将领域知识分为三类指令（Directives），并对应三个执行算子（Operators）：

*   **知识提取（KnowledgeParser）**：利用 LLM 将自然语言 Prompt 解析为结构化指令集：$S$（结构）、$F$（过滤）、$V$（验证）。
*   **结构算子（Structural Operator）**：
    *   **阶段**：上下文选择阶段。
    *   **作用**：根据“看哪里”的指令（如“MD&A 章节”、“表格”），在分块前对文档进行物理剪枝。
    *   **实现**：基于预处理阶段生成的结构化文档表示，匹配对应的标签，大幅缩小搜索空间。
*   **过滤算子（Filter Operator）**：
    *   **阶段**：推理引擎阶段。
    *   **作用**：根据“忽略什么”的指令（如“风险披露”），剔除语义无关的分块。
    *   **实现**：采用**模型级联（Model Cascade）**策略，使用轻量级小模型（SLM，如 4B 参数）对 Top-K 分块进行二分类过滤，仅将通过的分块传给昂贵的大模型。
*   **验证算子（Validate Operator）**：
    *   **阶段**：验证与排序阶段。
    *   **作用**：根据“如何验证”的指令（如“需包含稀释后每股收益，而非基本每股收益”），对多个候选答案进行重排序。
    *   **实现**：将验证指令分为正向（$V^+$）和负向（$V^-$）约束，计算候选答案与约束的嵌入向量相似度，奖励符合正向约束的答案，惩罚符合负向约束的答案。
*   **回退机制（Fallback Manager）**：
    *   为了防止领域知识不准确导致误删正确信息，Halo 监控运行时信号（如 LLM 全拒答或排序分差过小），一旦检测到算子失效，自动回退到全量文档处理或置信度排序模式。

---

#### 3. 实验设计
*   **数据集**：
    1.  **DocFinQA**：长篇金融 SEC 文件（平均 193K Token），侧重数值推理。
    2.  **NovelQA**：英文长篇小说（平均 230K Token），侧重叙事理解。
    3.  **QASPER**：科研论文（平均 7K Token），侧重信息查找。
*   **Benchmark 与对比方法**：
    *   **VanillaLLM**：单次调用处理全量上下文。
    *   **RAG**：基于嵌入检索的 Top-K 分块。
    *   **+DK (Domain Knowledge)**：在上述基准的 Prompt 中直接加入领域知识。
    *   **CoA (Chain-of-Agents)**：多智能体并行处理分块并汇总。
    *   **Halo (No DK)**：不带领域算子的 Halo 基础流水线。
*   **实验模型**：闭源模型使用 Claude Sonnet 4.5，开源模型使用 Qwen3-30B（主模型）和 Qwen3-4B（过滤模型）。

---

#### 4. 资源与算力
*   **算力平台**：闭源模型通过 Amazon AWS Bedrock API 调用；开源模型部署在本地，使用 **vLLM** 推理框架。
*   **具体配置**：文中未明确提及具体的 GPU 型号（如 A100/H100）数量及训练时长。由于该研究侧重于推理阶段的查询优化而非模型训练，其核心开销在于 API 调用成本和本地推理延迟。

---

#### 5. 实验数量与充分性
*   **实验规模**：在三个不同领域的代表性数据集上进行了测试，涵盖了从几千到几十万 Token 的不同长度。
*   **消融实验**：详细分析了 Structural、Filter、Validate 三个算子各自对准确率和成本的贡献。
*   **鲁棒性测试**：专门设计了“低质量/错误领域知识”场景，验证回退机制的有效性。
*   **成本分析**：提供了详尽的各阶段成本占比分析。
*   **评价**：实验设计较为充分且客观，通过跨模型（闭源 vs 开源）和跨领域验证，证明了方法的普适性。

---

#### 6. 主要结论与发现
*   **性能提升**：Halo 在 DocFinQA 上比基准方法准确率提升高达 **13%**。
*   **成本大幅降低**：相比 VanillaLLM，成本降低了 **4.8 倍**。
*   **以小博大**：使用 **Qwen3-30B + Halo** 的组合，其准确率接近 **Claude Sonnet 4.5** 的原生表现，但成本仅为后者的 **1/78**。
*   **算子协同**：结构算子和过滤算子主要负责降本，验证算子主要负责提质，三者结合产生了显著的增益。
*   **Prompt 局限性**：实验发现直接在 Prompt 中添加知识（+DK）有时反而会降低弱模型的表现，证明了“系统化执行”优于“提示词工程”。

---

#### 7. 优点
*   **自动化程度高**：无需用户手动构建复杂的 LLM 流水线，直接从现有 Prompt 中提取逻辑。
*   **架构解耦**：将领域知识与模型推理分离，使得系统可以灵活更换底层 LLM 或 SLM。
*   **鲁棒性设计**：引入回退机制，解决了领域知识可能带来的“负优化”风险。
*   **极高的性价比**：通过模型级联和结构剪枝，极大地释放了开源模型在长文本处理上的潜力。

---

#### 8. 不足与局限
*   **预处理依赖**：Structural 算子高度依赖于文档预解析（PDF 转结构化文本）的质量，若解析出错，后续优化将受限。
*   **领域局限**：在非结构化程度极高的文本（如文学小说）中，结构算子的增益明显低于金融或科研文档。
*   **回退开销**：虽然回退机制能保证准确率，但一旦触发回退，会导致该次查询的成本和延迟增加（因为需要重新执行）。
*   **实时性挑战**：多阶段处理（解析、过滤、生成、验证）虽然通过并行化缓解了延迟，但相比单次 RAG 仍有更复杂的逻辑开销。

（完）
