---
title: "Adaptive Chunking: Optimizing Chunking-Method Selection for RAG"
title_zh: 自适应分块：优化 RAG 的分块方法选择
authors: "Paulo Roberto de Moura Júnior, Jean Lelong, Annabelle Blangero"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.25333v1"
tags: ["query:sr"]
score: 8.0
evidence: 优化分块以提高检索效果
tldr: 本研究针对检索增强生成（RAG）中分块策略缺乏灵活性和评估标准的问题，提出了自适应分块（Adaptive Chunking）框架。该框架通过五项新颖的内在指标（如引用完整性、块内凝聚力等）为不同文档动态选择最优分块策略，并引入了LLM正则分割器和递归合并分割器。实验表明，该方法在不改变模型的情况下显著提升了RAG的回答准确率和成功率，为构建鲁棒的RAG系统提供了新路径。
selection_source: fresh_fetch
motivation: 现有的分块方法多为通用型，难以捕捉多样化文本的复杂结构，且缺乏专门的评估框架来独立衡量分块质量。
method: 提出一种自适应框架，基于五种新颖的内在指标评估分块质量，并结合新型LLM正则分割器和递归合并分割器进行策略优化。
result: "在多领域测试中，该框架将RAG回答准确率从约63%提升至72%，并使成功回答的问题数量增加了30%以上。"
conclusion: 通过内在指标引导的自适应分块策略能显著增强RAG系统的检索效果和生成质量，是优化RAG性能的有效手段。
---

## 摘要
检索增强生成 (RAG) 的有效性高度依赖于文档的分块方式，即为了索引和检索而将文档分割成较小的单元。然而，常用的“一刀切”方法往往无法捕捉到不同文本的细微结构和语义。尽管分块起着核心作用，但它缺乏专门的评估框架，导致难以独立于下游性能来评估和比较各种策略。我们通过引入“自适应分块”（Adaptive Chunking）来挑战这一范式，该框架根据五项新颖的、基于文档的内在指标为每个文档选择最合适的分块策略：引用完整性 (RC)、块内凝聚力 (ICC)、文档上下文连贯性 (DCC)、块完整性 (BI) 和尺寸合规性 (SC)，这些指标直接从关键维度评估分块质量。为了支持该框架，我们还引入了两种新的分块器：LLM-regex 分割器和“先分割后合并”的递归分割器，以及针对性的后处理技术。在涵盖法律、技术和社会科学领域的多元语料库上，我们的指标引导自适应方法显著提高了下游 RAG 的性能。在不改变模型或提示词的情况下，我们的框架提升了 RAG 的结果，将答案正确率从 62-64% 提高到 72%，并将成功回答的问题数量增加了 30% 以上（从 49 个增加到 65 个）。这些结果表明，在互补的内在指标套件引导下，这种具有文档感知能力的自适应分块为构建更稳健的 RAG 系统提供了一条实用且有效的途径。代码可在 https://github.com/ekimetrics/adaptive-chunking 获取。

## Abstract
The effectiveness of Retrieval-Augmented Generation (RAG) is highly dependent on how documents are chunked, that is, segmented into smaller units for indexing and retrieval. Yet, commonly used "one-size-fits-all" approaches often fail to capture the nuanced structure and semantics of diverse texts. Despite its central role, chunking lacks a dedicated evaluation framework, making it difficult to assess and compare strategies independently of downstream performance. We challenge this paradigm by introducing Adaptive Chunking, a framework that selects the most suitable chunking strategy for each document based on a set of five novel intrinsic, document-based metrics: References Completeness (RC), Intrachunk Cohesion (ICC), Document Contextual Coherence (DCC), Block Integrity (BI), and Size Compliance (SC), which directly assess chunking quality across key dimensions. To support this framework, we also introduce two new chunkers, an LLM-regex splitter and a split-then-merge recursive splitter, alongside targeted post-processing techniques. On a diverse corpus spanning legal, technical, and social science domains, our metric-guided adaptive method significantly improves downstream RAG performance. Without changing models or prompts, our framework increases RAG outcomes, raising answers correctness to 72% (from 62-64%) and increasing the number of successfully answered questions by over 30% (65 vs. 49). These results demonstrate that adaptive, document-aware chunking, guided by a complementary suite of intrinsic metrics, offers a practical and effective path to more robust RAG systems. Code available at https://github.com/ekimetrics/adaptive-chunking.

---

## 论文详细总结（自动生成）

这是一份关于论文《Adaptive Chunking: Optimizing Chunking-Method Selection for RAG》的深度结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：在检索增强生成（RAG）系统中，文档分块（Chunking）通常采用“一刀切”的固定策略（如固定字符数或递归分割），忽略了不同文档在结构和语义上的差异。
*   **研究动机**：
    *   分块质量直接影响检索精度，进而影响生成质量。
    *   目前缺乏专门的**内在评估指标**来独立衡量分块质量，通常只能通过下游任务（如检索准确率）间接评估。
    *   现有的语义分块或 LLM 分块方法计算成本高，且未必适用于所有文档类型。

### 2. 方法论：自适应分块框架
论文提出了 **Adaptive Chunking** 框架，其核心思想是：**针对每个文档，从多种分块策略中动态选择内在指标得分最高的一种。**

#### 关键技术细节：
*   **五项内在评估指标**：
    1.  **引用完整性 (RC)**：利用共指消解模型，衡量实体与其代词是否被切分到不同块中。
    2.  **块内凝聚力 (ICC)**：计算块内各句子嵌入向量与整个块嵌入向量的余弦相似度均值。
    3.  **文档上下文连贯性 (DCC)**：衡量块与其周围局部上下文（滑动窗口）的语义一致性。
    4.  **块完整性 (BI)**：评估是否破坏了文档的自然结构（如表格、段落、标题对）。
    5.  **尺寸合规性 (SC)**：衡量分块长度是否落在预设的有效区间内（如 100-1100 token）。
*   **新型分块器**：
    *   **LLM-regex 分割器**：利用 LLM 分析文档前部结构并生成正则表达式，再用正则对全文进行确定性分割。
    *   **先分割后合并递归分割器**：先按优先级（标题->段落->句子）细分，再贪婪合并以确保块大小达标并减少碎片。
*   **后处理技术**：引入“超大块再分割”和“微小块合并”步骤，确保所有分块在不破坏结构的前提下符合尺寸约束。

### 3. 实验设计
*   **数据集**：使用来自 Ekimetrics CLAIR 项目的 33 个真实 PDF 文档，涵盖**法律、技术、社会科学**三个领域，总计约 118 万 token。
*   **Benchmark**：针对每个文档生成 3 组高质量问答对（共 99 组），使用 GPT-4o 作为评判官。
*   **对比方法（Baselines）**：
    *   LangChain 递归字符分割器（默认参数及自定义参数）。
    *   基于页面的分割（原始版及后处理版）。
    *   语义分割器（Semantic Splitter）。
    *   基于固定句子数量的分割。

### 4. 资源与算力
*   **模型使用**：
    *   **嵌入模型**：Qwen3-Embedding-4B（检索）、jina-embeddings-v3（计算指标）。
    *   **重排序模型**：snowflake-arctic-embed-l-v2.0。
    *   **生成与评估**：GPT-4o (GPT-4.1) 和 GPT-5（用于正则生成实验）。
    *   **共指消解**：Maverick 模型。
*   **算力说明**：文中**未明确提及具体的 GPU 型号和数量**。但提供了运行耗时统计：自适应框架处理整个语料库约需 210 秒（含异步 LLM 调用），其中 DCC 指标计算和共指消解是最耗时的环节。

### 5. 实验数量与充分性
*   **实验规模**：
    *   对 9 种不同的分块方法进行了内在指标评估。
    *   对 3 种主要策略（自适应、LangChain 递归、原始页面）进行了下游 RAG 性能对比。
    *   进行了消解实验，验证后处理技术对尺寸合规性的提升。
*   **充分性评价**：实验设计较为**充分且客观**。它不仅涵盖了多领域的长文档，还通过统计学显著性检验（Wilcoxon 符号秩检验）验证了结果。通过固定检索和生成模型，隔离了分块策略对性能的单一影响。

### 6. 主要结论与发现
*   **自适应策略最优**：没有一种分块方法在所有文档上表现最佳。自适应框架通过动态选择，获得了最高的综合内在得分（91.07%）。
*   **下游性能显著提升**：相比 LangChain 默认分割，自适应分块将 RAG 的**答案正确率从 62% 提升至 72%**，成功回答的问题数量增加了 **32.7%**。
*   **指标相关性**：内在指标的微小提升（2-3%）在下游 RAG 任务中会产生放大效应（8-10% 的性能增益）。
*   **尺寸正则化的重要性**：简单的后处理（合并微小块）能显著减少检索槽位的浪费，提高上下文质量。

### 7. 优点
*   **解耦评估**：提出了不依赖下游 RAG 结果的分块质量评估体系，具有很强的工程参考价值。
*   **结构感知**：通过 Markdown 解析和块完整性（BI）指标，更好地保留了表格和标题的语义。
*   **实用性强**：引入的 LLM-regex 分割器结合了 LLM 的理解能力和正则的高效性，平衡了成本与效果。

### 8. 不足与局限
*   **计算开销**：计算 DCC 和 RC 指标较为耗时，可能不适合超大规模实时索引场景。
*   **语言限制**：由于共指消解模型（Maverick）的限制，RC 指标目前仅支持英文。
*   **超参数依赖**：指标的权重分配（目前是等权重）和滑动窗口大小等超参数仍需人工设定，可能存在进一步优化的空间。
*   **静态选择**：分块策略在索引时确定，无法根据用户查询的特性（如查询长短、类型）进行动态调整。

（完）
