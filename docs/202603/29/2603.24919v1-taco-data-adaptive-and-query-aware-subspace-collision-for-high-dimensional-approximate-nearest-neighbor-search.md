---
title: "TaCo: Data-adaptive and Query-aware Subspace Collision for High-dimensional Approximate Nearest Neighbor Search"
title_zh: TaCo：面向高维近似最近邻搜索的数据自适应且查询感知的子空间碰撞
authors: "Jiuqi Wei, Zhenyu Liao, Ruoyu Han, Quanqing Xu, Chuanhui Yang, Themis Palpanas"
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24919v1"
tags: ["query:sr"]
score: 8.0
evidence: 高维近似最近邻搜索
tldr: 本文针对高维近似最近邻搜索（ANNS）中的子空间碰撞框架存在的数据无关和查询盲目性问题，提出了TaCo方法。TaCo通过基于熵的子空间数据转换实现数据自适应的平衡划分，并结合查询感知的动态开销分配策略优化查询效率。实验表明，TaCo在索引速度、内存占用和查询吞吐量上均显著优于现有子空间碰撞方法，在索引与查询性能之间达到了极佳的平衡。
selection_source: fresh_fetch
motivation: 现有的子空间碰撞框架由于缺乏对数据分布和查询特征的感知，导致索引构建不平衡且查询开销浪费。
method: 提出了一种基于熵的子空间定向数据转换机制和查询感知的动态开销分配策略，实现了数据自适应与查询感知的子空间碰撞。
result: "在真实数据集上，TaCo相比现有SOTA子空间碰撞方法实现了高达8倍的索引加速、减少了40%的内存占用，并提升了1.5倍的查询吞吐量。"
conclusion: TaCo通过引入数据自适应和查询感知机制，显著提升了高维ANNS的效率，是子空间碰撞范式下的先进解决方案。
---

## 摘要
高维欧几里得空间中的近似最近邻搜索（ANNS）是一个具有广泛应用的基础性问题。子空间碰撞（Subspace Collision）是新提出的一种 ANNS 框架，它为相似性搜索提供了一种新范式，并实现了卓越的索引和查询性能。然而，子空间碰撞框架目前仍处于数据无关（data-agnostic）且查询无感知（query-oblivious）的状态，导致索引构建不平衡以及查询开销浪费。在本文中，我们从两个方面解决了这些局限性：首先，我们设计了一种面向子空间的数据转换机制，通过对转换后数据的每个子空间计算的熵进行平均，确保了（在信息论意义上的）平衡子空间划分，并实现了数据自适应的子空间碰撞；其次，我们提出了查询感知且可扩展的查询策略，能够为每个查询动态分配开销，并加速子空间内的碰撞探测。基于这些想法，我们提出了一种新型的数据自适应且查询感知的子空间碰撞方法，简称 TaCo，它在保持索引和查询性能之间出色平衡的同时，实现了高效且准确的 ANN 搜索。在真实数据集上的广泛实验表明，与最先进的子空间碰撞方法相比，TaCo 在索引构建上实现了高达 8 倍的加速，内存占用减少至 0.6 倍，同时查询吞吐量提升了 1.5 倍以上。此外，即使与子空间碰撞范式之外的先进方法相比，TaCo 也实现了最先进的索引性能，并在索引和查询效率之间提供了有效的平衡。本文发表于 SIGMOD 2026。

## Abstract
Approximate Nearest Neighbor Search (ANNS) in high-dimensional Euclidean spaces is a fundamental problem with broad applications. Subspace Collision is a newly proposed ANNS framework that provides a novel paradigm for similarity search and achieves superior indexing and query performance. However, the subspace collision framework remains data-agnostic and query-oblivious, resulting in imbalanced index construction and wasted query overhead. In this paper, we address these limitations from two aspects: first, we design a subspace-oriented data transformation mechanism by averaging the entropies computed over each subspace of the transformed data, which ensures balanced subspace partitioning (in an information theoretical sense) and enables data-adaptive subspace collision; second, we present query-aware and scalable query strategies that dynamically allocate overhead for each query and accelerate collision probing within subspaces. Building on these ideas, we propose a novel data-adaptive and query-aware subspace collision method, abbreviated as TaCo, which achieves efficient and accurate ANN search while maintaining an excellent balance between indexing and query performance. Extensive experiments on real-world datasets demonstrate that, when compared to state-of-the-art subspace collision methods, TaCo achieves up to 8x speedup in indexing and reduces to 0.6x memory footprint, while achieving over 1.5x query throughput. Moreover, TaCo achieves state-of-the-art indexing performance and provides an effective balance between indexing and query efficiency, even when compared with advanced methods beyond the subspace-collision paradigm. This paper was published in SIGMOD 2026.

---

## 论文详细总结（自动生成）

### TaCo：面向高维近似最近邻搜索的数据自适应且查询感知的子空间碰撞

#### 1. 核心问题与研究动机
*   **核心问题**：在高维欧几里得空间中进行近似最近邻搜索（ANNS）时，现有的“子空间碰撞”（Subspace Collision）框架（如 SuCo）虽然表现优异，但存在三个主要缺陷：
    1.  **数据无关性**：采用朴素的均匀维度划分，忽略了数据的内在分布，导致子空间统计特性不平衡。
    2.  **查询盲目性**：对所有查询采用统一的搜索开销，忽略了不同查询之间难度和判别度的巨大差异。
    3.  **扩展性瓶颈**：其倒排多索引（IMI）的激活算法具有线性复杂度，在精细索引下会成为效率瓶颈。
*   **研究动机**：通过引入信息论中的熵平衡和动态查询感知机制，提升子空间碰撞框架的精度、效率及在不同数据集上的自适应能力。

#### 2. 方法论：核心思想与关键技术
TaCo（**Ta**sk-adaptive and **Co**llision）方法主要包含以下三个核心组件：
*   **面向子空间的数据转换（基于熵平均）**：
    *   **核心思想**：通过线性变换将原始数据投影到多个子空间，使得每个子空间的微分熵（Differential Entropy）尽可能相等，从而实现信息在子空间间的平衡分布。
    *   **技术细节**：将问题建模为约束最小-最大优化问题，利用“特征系统分配”（Eigensystem Allocation）算法，根据协方差矩阵的特征值和特征向量进行贪婪分配。
    *   **效果**：该转换不仅实现了数据自适应，还显著降低了维度（降维率达 40%~96%）。
*   **可扩展的动态激活算法**：
    *   **核心思想**：针对 IMI 索引结构，将原有的线性搜索优化为基于**最小堆（Min-heap）**的动态激活机制。
    *   **技术细节**：将查询复杂度从 $O(\sqrt{K})$ 降低到 $O(1)$，支持在更精细的索引粒度下保持高效的碰撞探测。
*   **查询感知的候选集选择**：
    *   **核心思想**：根据每个查询产生的 SC-score（子空间碰撞得分）分布，动态调整重排序（Re-rank）的候选集大小。
    *   **技术细节**：对于判别度高的查询，仅需重排少量高分点；对于判别度低的查询，则自动扩大候选集以保证召回率。

#### 3. 实验设计
*   **数据集**：使用了 5 个主流真实世界数据集：DEEP1M (256维)、GIST1M (960维)、SIFT10M (128维)、Yandex DEEP10M (96维)、Microsoft SPACEV10M (100维)。
*   **对比方法（Benchmark）**：
    *   **子空间碰撞类**：SuCo (SOTA)、SC-Linear (基准) 以及三种消融变体（SuCo-DT, SuCo-CS, SuCo-QS）。
    *   **非子空间碰撞类**：包括量化类（IMI-OPQ, IVF-RaBitQ）、哈希类（DET-LSH）和图算法类（HNSW, MIRAGE, SHG）。
*   **评估指标**：索引时间、内存占用、查询时间、每秒查询数（QPS）、召回率（Recall）和平均相对误差（MRE）。

#### 4. 资源与算力
*   **硬件环境**：服务器配备两颗 **AMD EPYC 9554 CPU** (3.10 GHz)，共 128 核心，**756 GB 内存**。
*   **软件实现**：使用 C++ 开发，OpenMP/Pthreads 并行化，利用 SIMD 指令加速计算，编译器开启 -O3 优化。
*   **算力说明**：论文未提及使用 GPU，属于纯 CPU 密集型向量检索研究。

#### 5. 实验数量与充分性
*   **实验规模**：涵盖了从 100 万到 1000 万规模的数据集，测试了 $k=1$ 到 $100$ 的不同场景。
*   **消融实验**：针对数据转换（DT）、候选集选择（CS）和查询策略（QS）分别做了详细的消融对比，验证了每个模块的贡献。
*   **参数敏感性**：对子空间数量 $N_s$、子空间维度 $s$、聚类数 $K$ 等关键参数进行了详尽的调优实验。
*   **充分性评价**：实验设计严谨，对比了不同范式的 SOTA 方法，并引入了“累积查询成本”分析，能够客观反映算法在实际部署中的综合表现。

#### 6. 主要结论与发现
*   **性能飞跃**：相比 SuCo，TaCo 实现了高达 **8 倍**的索引加速，内存占用减少了 **40%**，查询吞吐量提升了 **1.5 倍**以上。
*   **降维红利**：通过熵平均转换，在保持精度的前提下大幅压缩了维度，这是效率提升的关键。
*   **平衡性**：TaCo 在索引构建速度和查询效率之间达到了极佳的平衡。在处理前 5万-8万个查询时，其总成本（索引+查询）远低于 HNSW 等图算法。
*   **鲁棒性**：在 GIST1M 等“困难”数据集上，TaCo 的子空间碰撞机制比图算法更具容错性，不易陷入局部最优。

#### 7. 优点与亮点
*   **理论支撑强**：引入信息论中的微分熵来解决子空间划分问题，并提供了随机矩阵理论背景下的性能保证证明。
*   **降维效果显著**：在不损失关键邻域关系的前提下，极大地减少了计算量。
*   **动态感知**：打破了传统方法“一刀切”的查询处理模式，实现了真正的查询感知开销分配。

#### 8. 不足与局限
*   **预处理开销**：虽然索引构建极快，但数据转换涉及协方差矩阵计算和特征分解，对于超大规模（如十亿级）数据的预处理成本未被计入索引时间。
*   **内存依赖**：作为内存索引方法，虽然比图算法省内存，但在处理海量数据时仍受限于物理内存容量，尚未讨论磁盘驻留的扩展方案。
*   **参数依赖**：虽然具有自适应性，但仍需预设子空间数量等超参数，不同数据集的最优参数组合仍需少量经验调试。

（完）
