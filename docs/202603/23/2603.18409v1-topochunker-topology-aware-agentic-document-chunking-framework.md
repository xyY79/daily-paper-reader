---
title: "TopoChunker: Topology-Aware Agentic Document Chunking Framework"
title_zh: TopoChunker：拓扑感知的智能体文档分块框架
authors: Xiaoyu Liu
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18409v1"
tags: ["query:sr"]
score: 8.0
evidence: 拓扑感知的文档分块以保留结构层级
tldr: 针对检索增强生成（RAG）中传统线性分块导致语义碎片化的问题，本文提出TopoChunker框架。该框架通过结构化中间表示（SIR）保留文档的拓扑层级，并采用双代理架构（Inspector和Refiner）在保证结构完整性的同时优化计算成本。实验表明，该方法在生成准确率和检索召回率上均显著优于现有LLM基线，且大幅降低了Token开销，为结构感知型RAG提供了高效方案。
selection_source: fresh_fetch
motivation: 传统的线性分块方法破坏了文档内在的拓扑结构，导致语义碎片化并降低了下游检索的质量。
method: 提出一种基于双代理架构的框架，利用结构化中间表示（SIR）显式保留跨段落依赖关系，并通过动态路由和容量审计优化提取路径。
result: "在多个数据集上，该方法比最强LLM基线在生成准确率上提升了8.0%，Recall@3达到83.26%，且Token开销降低了23.5%。"
conclusion: TopoChunker证明了通过拓扑感知和代理协作可以有效解决文档分块中的语义丢失问题，实现了性能与成本的平衡。
---

## 摘要
现有的检索增强生成（RAG）文档分块方法通常将文本线性化。这种强制线性化剥离了内在的拓扑层级结构，导致“语义碎片化”，从而降低了下游检索的质量。在本文中，我们提出了 TopoChunker，这是一个智能体框架，它将异构文档映射到结构化中间表示（SIR）上，以显式保留跨段依赖关系。为了平衡结构保真度与计算成本，TopoChunker 采用了双智能体架构。检查员智能体（Inspector Agent）通过成本优化的提取路径动态路由文档，而精炼者智能体（Refiner Agent）则执行容量审计和拓扑上下文消歧，以重建层级谱系。在非结构化叙事（GutenQA）和复杂报告（GovReport）上的评估表明，TopoChunker 展示了最先进的性能。它在绝对生成准确率上比最强的基于大语言模型（LLM）的基准模型高出 8.0%，并实现了 83.26% 的 Recall@3，同时减少了 23.5% 的 Token 开销，为结构感知型 RAG 提供了一种可扩展的方法。

## Abstract
Current document chunking methods for Retrieval-Augmented Generation (RAG) typically linearize text. This forced linearization strips away intrinsic topological hierarchies, creating ``semantic fragmentation'' that degrades downstream retrieval quality. In this paper, we propose TopoChunker, an agentic framework that maps heterogeneous documents onto a Structured Intermediate Representation (SIR) to explicitly preserve cross-segment dependencies. To balance structural fidelity with computational cost, TopoChunker employs a dual-agent architecture. An Inspector Agent dynamically routes documents through cost-optimized extraction paths, while a Refiner Agent performs capacity auditing and topological context disambiguation to reconstruct hierarchical lineage. Evaluated on unstructured narratives (GutenQA) and complex reports (GovReport), TopoChunker demonstrates state-of-the-art performance. It outperforms the strongest LLM-based baseline by 8.0% in absolute generation accuracy and achieves an 83.26% Recall@3, while simultaneously reducing token overhead by 23.5%, offering a scalable approach for structure-aware RAG.

---

## 论文详细总结（自动生成）

### TopoChunker: 拓扑感知的智能体文档分块框架论文总结

#### 1. 核心问题与研究动机
在检索增强生成（RAG）流程中，文档分块（Chunking）是决定检索质量的基础。当前主流方法（如固定长度分块或简单的语义分块）存在**“强制线性化”**的问题，即将复杂的、具有层级结构的文档（如带标题、表格的PDF）强行压平为线性文本流。这导致了：
*   **语义碎片化**：破坏了文档内在的拓扑层级，产生“语义孤岛”。
*   **指代不明**：分块后，代词（如“它”、“该公司”）与其在上下文中的指代对象被切断。
*   **计算冗余**：现有的LLM分块器对所有文本一律采用高昂的推理开销，缺乏针对文档复杂度的动态调整。

#### 2. 方法论
TopoChunker 提出了一个基于**双智能体（Dual-Agent）**架构的框架，核心思想是将文档映射为**结构化中间表示（SIR）**，以显式保留跨段落的依赖关系。

*   **认知感知与动态路由（Inspector Agent）**：
    *   **主动探测**：该智能体不直接读取全文，而是通过轻量级API（元数据解析、布局密度扫描）提取文档的“拓扑指纹”。
    *   **三路径路由**：根据复杂度将文档分配至：1) **结构规则路径**（针对标准标题层级）；2) **语义流路径**（针对无结构的叙事文本）；3) **视觉拓扑路径**（针对扫描件或复杂布局，调用VLM）。
*   **拓扑建模与SIR构建**：
    *   使用栈遍历算法构建内存中的树状拓扑（SIR）。
    *   **原子性锁定（Atomicity Locking）**：确保不可分割的单元（如跨页表格）不被物理切断。
    *   **拓扑谱系（Topological Lineage）**：记录每个节点的全局坐标（如 /章1/节3），使叶子节点能继承宏观上下文。
*   **语义精炼与上下文装配（Refiner Agent）**：
    *   **容量审计**：对超过Token阈值的节点进行语义切分。
    *   **上下文消歧**：通过`SIR_Query`工具在树结构中向上追溯，解析悬挂代词和孤立术语，并将解析后的上下文显式注入分块中。

#### 3. 实验设计
*   **数据集**：
    *   **GutenQA**：包含100本公有领域叙事书籍，3000个问答对，代表非结构化文本。
    *   **GovReport**：复杂的政府报告，具有深层级结构，抽取500个查询进行评估。
*   **对比方法（Benchmarks）**：
    *   固定大小分块（FC200）。
    *   Semantic Chunker（基于嵌入相似度）。
    *   Proposition-Level Chunking（原子事实分解）。
    *   **LumberChunker**：目前最强的基于LLM的分块基线。
*   **评估指标**：Recall@k、DCG@k（检索质量）以及生成准确率（LLM-as-a-judge，使用GPT-4并经过人工审计验证）。

#### 4. 资源与算力
*   **模型底座**：使用 **DeepSeek-R1** 作为 Inspector 和 Refiner 智能体的推理引擎；使用 **BGE-m3** 作为嵌入模型。
*   **算力说明**：论文**未明确说明**具体的GPU型号、数量及训练/推理总时长。但提到通过动态路由减少了23.5%的Token消耗，暗示了其在推理效率上的优化。

#### 5. 实验数量与充分性
*   **实验规模**：在两个性质截然不同的数据集（叙事类 vs. 报告类）上进行了端到端测试。
*   **消融实验**：分别验证了 Refiner Agent（提升语义质量）和 Inspector Agent（提升路由效率）的贡献。
*   **客观性**：引入了人工审计（100组样本），结果显示人工标注与GPT-4评分的一致性达92%，证明了自动化评估的可靠性。实验对比了从传统规则到最新LLM代理的多种范式，覆盖面较广。

#### 6. 主要结论与发现
*   **性能领先**：TopoChunker 在生成准确率上比最强基线 LumberChunker 高出 **8.0%**，在 GovReport 上实现了 **83.26%** 的 Recall@3。
*   **成本优化**：通过动态路由，避免了对简单文档使用昂贵的VLM或全量LLM扫描，平均 Token 开销降低了 **23.5%**。
*   **拓扑的重要性**：实验证明，显式注入拓扑元数据（如父标题、实体定义）能显著减少 RAG 中的幻觉，使生成的回答更具结构化支撑。

#### 7. 优点
*   **架构优雅**：将物理解析与语义推理解耦，通过 SIR 解决了 RAG 中长期的“语义孤岛”问题。
*   **成本感知**：Inspector Agent 的主动探测机制非常实用，解决了“用大炮打蚊子”的计算浪费问题。
*   **上下文恢复**：Refiner Agent 的消歧功能极大地增强了分块的自包含性（Self-contained），对下游检索极其友好。

#### 8. 不足与局限
*   **上游依赖**：对于复杂布局（路径3），高度依赖外部 VLM 提取器（如 MinerU），若 OCR 或布局识别出错，错误会向下传播。
*   **推理延迟**：尽管节省了 Token，但智能体架构涉及多次顺序 API 调用（ReAct 循环），其推理延迟（Latency）高于传统的确定性算法，可能不适用于实时性要求极高的场景。
*   **独立研究局限**：作为独立研究者的工作，缺乏在大规模工业级异构数据集上的压力测试。

（完）
