---
title: "Towards Intelligent Geospatial Data Discovery: a knowledge graph-driven multi-agent framework powered by large language models"
title_zh: 迈向智能地理空间数据发现：一种由大语言模型驱动的知识图谱驱动多智能体框架
authors: "Ruixiang Liu, Zhenlong Li, Ali Khosravi Kazazi"
date: 2026-03-21
pdf: "https://arxiv.org/pdf/2603.20670v1"
tags: ["query:sr"]
score: 8.0
evidence: 利用知识图谱和大模型进行智能地理空间数据发现
tldr: 针对地理空间数据发现中存在的异构性、语义不一致及关键词搜索局限性，本研究提出了一种由大语言模型驱动的知识图谱多智能体框架。该框架通过统一元数据本体构建知识图谱，并利用多智能体协作实现意图解析与结果合成。实验证明，该方法显著提升了检索准确率和透明度，为下一代智能空间数据基础设施和自主GIS奠定了基础。
selection_source: fresh_fetch
motivation: 传统的地理空间数据检索依赖关键词搜索，难以处理海量异构数据中的复杂语义和用户意图。
method: 提出一种结合统一元数据本体、知识图谱与大语言模型多智能体协作的语义检索框架。
result: 实验表明，该框架在检索意图匹配、排序质量、召回率及过程透明度方面均优于传统系统。
conclusion: 该研究推动了地理空间数据发现向语义化和智能化转型，是实现自主GIS愿景的关键一步。
---

## 摘要
地理空间数据在规模、多样性和增长速度上的快速增长，构建了高度分布式、异构且语义不一致的数据生态系统。现有的数据目录、门户和基础设施仍主要依赖于语义支持有限的关键词搜索，这往往无法捕捉用户意图，导致检索性能较弱。为应对这些挑战，本研究提出了一种由大语言模型驱动的知识图谱驱动多智能体框架，用于智能地理空间数据发现。该框架引入了统一的地理空间元数据本体作为语义中介层，以对齐跨平台的异构元数据标准，并构建了地理空间元数据知识图谱，从而对数据集及其多维关系进行显式建模。基于结构化表示，该框架采用多智能体协作架构来执行意图解析、知识图谱检索和答案合成，形成了从用户查询到结果的可解释闭环发现过程。代表性用例和性能评估结果表明，与传统系统相比，该框架显著提高了意图匹配准确度、排序质量、召回率和发现透明度。本研究推动地理空间数据发现向更具语义化、意图感知和智能化的范式演进，为下一代智能自主空间数据基础设施提供了实践基础，并为“自主 GIS”（Autonomous GIS）的宏伟愿景做出了贡献。

## Abstract
The rapid growth in the volume, variety, and velocity of geospatial data has created data ecosystems that are highly distributed, heterogeneous, and semantically inconsistent. Existing data catalogs, portals, and infrastructures still rely largely on keyword-based search with limited semantic support, which often fails to capture user intent and leads to weak retrieval performance. To address these challenges, this study proposes a knowledge graph-driven multi-agent framework for intelligent geospatial data discovery, powered by large language models. The framework introduces a unified geospatial metadata ontology as a semantic mediation layer to align heterogeneous metadata standards across platforms and constructs a geospatial metadata knowledge graph to explicitly model datasets and their multidimensional relationships. Building on the structured representation, the framework adopts a multi-agent collaborative architecture to perform intent parsing, knowledge graph retrieval, and answer synthesis, forming an interpretable and closed-loop discovery process from user queries to results. Results from representative use cases and performance evaluation show that the framework substantially improves intent matching accuracy, ranking quality, recall, and discovery transparency compared with traditional systems. This study advances geospatial data discovery toward a more semantic, intent-aware, and intelligent paradigm, providing a practical foundation for next-generation intelligent and autonomous spatial data infrastructures and contributing to the broader vision of Autonomous GIS.

---

## 论文详细总结（自动生成）

### 论文结构化总结：迈向智能地理空间数据发现

#### 1. 核心问题与整体含义（研究动机和背景）
随着地理空间数据在规模、种类和速度上的爆发式增长，数据生态系统呈现出高度分布式、异构化且语义不一致的特征。
*   **研究动机**：现有的地理空间数据门户（如 Data.gov）主要依赖基于关键词的检索，缺乏对空间、时间和主题语义的深度理解，导致难以准确捕捉用户意图，检索结果的查准率和查全率较低。
*   **核心问题**：如何在异构元数据标准（如 ISO 19115, STAC, FGDC 等）之间建立语义对齐，并利用大语言模型（LLM）的推理能力实现从自然语言查询到结构化、可解释的数据发现过程。

#### 2. 方法论：核心思想与关键技术
该研究提出了一种**知识图谱驱动的多智能体框架**，其核心流程如下：
*   **统一元数据本体（Ontology）设计**：设计了一个包含数据集（Dataset）、主题（Topic）、空间（Space）、时间（Time）、组织（Organization）、格式（Format）和许可（License）等核心实体的语义中介层，用于对齐不同平台的元数据。
*   **地理空间元数据知识图谱（GMKG）构建**：
    *   通过解析器将异构元数据映射至本体。
    *   利用 LLM 自动生成主题实体，并使用向量嵌入（Embedding）技术处理语义相似性。
    *   建立空间索引（Bounding Box）和时间索引。
*   **多智能体协作架构**：
    *   **意图解析 Agent**：将自然语言转化为结构化的 `GeoIntent`，包含置信度评估和人机交互（Human-in-the-loop）纠偏机制。
    *   **图检索 Agent**：执行向量相似度匹配、硬约束过滤（空间相交、时间重叠）和多维加权评分。
    *   **答案合成 Agent**：基于 LLM 对 Top-K 结果进行重排序（Reranking），生成匹配理由，并提供知识子图可视化以增强透明度。

#### 3. 实验设计
*   **数据集/场景**：整合了 6 个主流数据目录（包括 Data.gov 的地理空间部分、PASDA、以及 4 个 STAC API 节点），共计 **264,177 个数据集**，构建了拥有超过 1200 万个关系的知识图谱。
*   **Benchmark**：构建了包含 **100 个测试查询** 的基准集，按约束复杂度分为四级（从单一主题到包含时空、组织、格式等多重约束）。
*   **对比方法**：
    1.  **Data.gov 官方门户**（基准）。
    2.  **IGDD-G**（仅包含意图解析和图检索的消融版本）。
    3.  **IGDD-G&A**（完整框架，包含答案合成 Agent）。
*   **评估指标**：精确意图匹配率（EIMR）、归一化折扣增益（NDCG@10）和召回率（Recall@20）。

#### 4. 资源与算力
*   **模型使用**：底层 LLM 采用了 OpenAI 的 **GPT-4o**（文中表述为 GPT-5.2，推测为特定版本或笔误），向量模型使用 `text-embedding-3-large`。
*   **算力与成本**：
    *   论文未明确提及具体的 GPU 硬件型号和数量。
    *   **推理效率**：单次查询的完整发现周期约为 **30-40 秒**。
    *   **Token 消耗**：平均每次查询消耗约 **11,000 个 tokens**。
    *   **数据库**：部署在高性能服务器上的 Neo4j 图数据库。

#### 5. 实验数量与充分性
*   **实验规模**：针对 100 个查询进行了深度评估，并由两名地理信息科学专业的博士生对 **3,646 个池化数据集** 进行了人工相关性标注（0-3 级评分）。
*   **充分性**：实验涵盖了消融实验（验证合成 Agent 的作用）和跨复杂度等级的对比实验。
*   **客观性**：采用了“池化评估”（Pooling）方法，确保了不同系统在同一基准下进行公平对比，且人工标注过程遵循标准化准则，具有较高的客观性。

#### 6. 主要结论与发现
*   **性能飞跃**：完整框架（IGDD-G&A）的 NDCG@10 达到 **85.22%**，比 Data.gov（43.65%）提升了 **41.57%**。
*   **召回能力**：在 Data.gov 单一数据源下，该框架的 Recall@20 达到 66.54%，远高于原始门户的 34.61%。
*   **复杂度适应性**：随着查询约束变复杂，传统系统的性能急剧下降，而该框架表现出极强的鲁棒性，尤其在 Level 4 复杂查询下优势最明显。
*   **意图解析**：意图解析 Agent 的准确率（EIMR）高达 **99.27%**。

#### 7. 优点
*   **语义对齐能力**：成功解决了跨标准元数据的语义鸿沟。
*   **可解释性与透明度**：通过 Agent 生成匹配理由和知识子图，让用户理解“为什么返回这些结果”。
*   **人机协作**：引入置信度阈值触发的人机交互，有效解决了模糊查询的歧义问题。
*   **端到端闭环**：实现了从自然语言到最终答案合成的完整智能化流程。

#### 8. 不足与局限
*   **数据预处理缺失**：目前仅停留在“发现”阶段，尚未实现从发现到“分析就绪数据”（ARD）生成的自动化衔接。
*   **分辨率与尺度问题**：框架尚未深入处理地理空间数据特有的时空分辨率匹配和尺度兼容性问题。
*   **成本与延迟**：依赖商业大模型 API，单次查询的 Token 消耗和响应时间（40秒）对于高并发实时应用仍有优化空间。
*   **覆盖范围**：虽然整合了 26 万数据集，但相比全球海量的地理空间资源，仍需进一步扩展数据源。

（完）
