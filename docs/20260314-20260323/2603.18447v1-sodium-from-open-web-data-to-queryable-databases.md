---
title: "SODIUM: From Open Web Data to Queryable Databases"
title_zh: SODIUM：从开放网络数据到可查询数据库
authors: "Chuxuan Hu, Philip Li, Maxwell Yang, Daniel Kang"
date: 2026-03-19
pdf: "https://arxiv.org/pdf/2603.18447v1"
tags: ["query:sr"]
score: 9.0
evidence: 对开放网络数据的系统性探索与发现
tldr: 本研究针对领域专家在从开放网络获取结构化数据进行分析时面临的繁琐过程，提出了SODIUM任务，旨在将网络视为潜在数据库并进行系统实例化。为此，作者构建了包含105个任务的SODIUM-Bench基准测试。针对现有AI代理表现不佳的问题，开发了SODIUM-Agent，通过ATP-BFS算法和多代理协作实现深度网络探索与结构化信息提取，显著提升了自动化构建可查询数据库的能力。
selection_source: fresh_fetch
motivation: 领域专家在分析前需耗费大量精力从海量且分散的网络资源中搜索、提取和组织原始数据。
method: 提出了SODIUM-Agent多代理系统，利用ATP-BFS算法进行深度网络探索，并结合缓存管理器优化导航路径与信息提取。
result: "SODIUM-Agent在SODIUM-Bench上达到了91.1%的准确率，性能是现有最强基准测试的近两倍。"
conclusion: 该研究通过系统化的任务定义、基准测试和创新的代理架构，有效解决了从开放网络自动化构建结构化数据库的难题。
---

## 摘要
在研究过程中，领域专家经常提出分析性问题，其答案需要整合来自广泛网络来源的数据。因此，在开始分析之前，他们必须投入大量精力来搜索、提取和组织原始数据。我们将这一过程形式化为 SODIUM 任务，其中我们将网络等开放领域概念化为潜在数据库，必须对其进行系统实例化以支持下游查询。解决 SODIUM 任务需要：(1) 对开放网络进行深入且专业的探索；(2) 利用结构相关性进行系统的信息提取；(3) 将收集到的信息整合为连贯、可查询的数据库实例。为了量化自动化 SODIUM 的挑战，我们构建了 SODIUM-Bench，这是一个包含 105 个任务的基准测试，这些任务源自 6 个领域的已发表学术论文，要求系统探索开放网络，从不同来源收集数据并汇总到结构化表格中。现有系统在 SODIUM 任务上表现挣扎：我们在 SODIUM-Bench 上评估了 6 个先进的 AI 智能体，最强的基准模型仅达到 46.5% 的准确率。为了弥补这一差距，我们开发了 SODIUM-Agent，这是一个由网络探索器和缓存管理器组成的多智能体系统。在所提出的 ATP-BFS 算法的驱动下，并通过对缓存源和导航路径的原则性管理进行优化，SODIUM-Agent 能够进行深入且全面的网络探索，并执行结构连贯的信息提取。SODIUM-Agent 在 SODIUM-Bench 上实现了 91.1% 的准确率，性能优于最强基准模型约 2 倍，优于最弱基准模型高达 73 倍。

## Abstract
During research, domain experts often ask analytical questions whose answers require integrating data from a wide range of web sources. Thus, they must spend substantial effort searching, extracting, and organizing raw data before analysis can begin. We formalize this process as the SODIUM task, where we conceptualize open domains such as the web as latent databases that must be systematically instantiated to support downstream querying. Solving SODIUM requires (1) conducting in-depth and specialized exploration of the open web, which is further strengthened by (2) exploiting structural correlations for systematic information extraction and (3) integrating collected information into coherent, queryable database instances.   To quantify the challenges in automating SODIUM, we construct SODIUM-Bench, a benchmark of 105 tasks derived from published academic papers across 6 domains, where systems are tasked with exploring the open web to collect and aggregate data from diverse sources into structured tables. Existing systems struggle with SODIUM tasks: we evaluate 6 advanced AI agents on SODIUM-Bench, with the strongest baseline achieving only 46.5% accuracy. To bridge this gap, we develop SODIUM-Agent, a multi-agent system composed of a web explorer and a cache manager. Powered by our proposed ATP-BFS algorithm and optimized through principled management of cached sources and navigation paths, SODIUM-Agent conducts deep and comprehensive web exploration and performs structurally coherent information extraction. SODIUM-Agent achieves 91.1% accuracy on SODIUM-Bench, outperforming the strongest baseline by approximately 2 times and the weakest by up to 73 times.

---

## 论文详细总结（自动生成）

### 《SODIUM：从开放网络数据到可查询数据库》论文深度总结

#### 1. 论文的核心问题与整体含义
*   **研究动机**：领域专家（如人口统计学家、经济学家）在研究中常需从政府门户、国际组织官网等开放网络资源中提取数据。然而，这些数据往往分散在深层嵌套的网页、多个报告或不同年份的链接中。
*   **核心问题**：现有的 AI 智能体（Agent）在处理此类任务时面临三大挑战：(1) 难以进行深层、专业的网页探索；(2) 无法利用表格单元格之间的结构相关性（如行、列的规律性）；(3) 难以保证提取数据在整合为数据库时的全局一致性。
*   **任务定义**：论文提出了 **SODIUM 任务**（Structuring Open DomaIn Unstructured Data into Materialized Databases），即将开放网络视为一个“潜在数据库”，通过系统化的探索将其实例化为可查询的结构化表格。

#### 2. 论文提出的方法论
论文开发了 **SodiumAgent**，一个由“网页探索器”和“缓存管理器”构成的多智能体系统。
*   **核心算法：ATP-BFS（增强后剪枝的广度优先搜索）**：
    *   **Augment（增强）**：通过推断 URL 模式（如将 2024 修改为 2025）主动生成可能存在的候选链接。
    *   **Prune（剪枝）**：根据查询相关性，从海量链接中筛选出最有潜力的 $K$ 个链接，防止搜索空间爆炸。
    *   **BFS 遍历**：层级化探索网页，支持处理静态 HTML、动态加载页面（通过模拟点击和交互）以及 PDF/图片文件。
*   **关键技术细节**：
    *   **Page Explorer**：具备自适应能力，能识别页面是否需要动态交互，并能自主构建 MCP（Model Context Protocol）工具来操作复杂的 UI 组件。
    *   **Cache Manager（缓存管理器）**：利用表格的结构依赖。如果左侧或上方单元格已填好，系统会优先复用它们的导航路径或来源页面，从而大幅减少重复搜索并提升一致性。

#### 3. 实验设计
*   **数据集/基准测试**：构建了 **SodiumBench**。
    *   **来源**：从 6 个领域（人口统计、体育、金融、经济、食品、气候）的 48 篇已发表学术论文中提取了 105 个真实分析任务。
    *   **规模**：包含 458 个主键值、506 个属性，共计 2149 个待填充单元格。
*   **对比方法**：评估了 6 个最先进的 AI 智能体：**AG2、AutoGPT、AutoGen、OpenAI ResearchBot、WebVoyager、Open Deep Research**。
*   **评估指标**：包括单元格准确率（Cell Acc）、行/列准确率（Row/Col Acc）以及最终的表格级准确率（Table Acc）。

#### 4. 资源与算力
*   **模型使用**：实验主要基于 **gpt-5-2025-08-07**（注：论文设定背景为 2026 年）作为底层大语言模型。
*   **算力说明**：文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该系统属于基于 API 的智能体框架，其核心开销在于 API 调用成本。论文提到 SodiumAgent 通过缓存机制将每个单元格的平均成本从 1.36 美元降低到了 0.41 美元。

#### 5. 实验数量与充分性
*   **实验规模**：针对 105 个任务进行了详尽测试，涉及超过 2000 次独立的数据检索操作。
*   **实验维度**：
    *   **消融实验**：对比了独立 Web Explorer 与完整 SodiumAgent（含缓存）的性能差异。
    *   **深度分析**：测试了不同搜索深度（1 层到 6 层以上）对准确率的影响。
    *   **缓存效用分析**：量化了路径复用对成本和准确率的贡献。
*   **客观性与公平性**：使用了“精确匹配”和“LLM-as-a-Judge”双重评估标准，并为所有基准模型提供了相同的上下文信息，实验设计较为严谨、客观。

#### 6. 论文的主要结论与发现
*   **性能飞跃**：SodiumAgent 达到了 **91.1%** 的任务准确率，而最强的基准模型（AG2）仅为 46.5%，性能提升近 2 倍。
*   **结构化推理是瓶颈**：现有 Agent 在提取单个单元格时表现尚可，但在保证整行或整表一致性（E1, E2 维度）时准确率大幅下降，而 SodiumAgent 通过缓存和路径复用克服了这一问题。
*   **搜索深度稳定性**：SodiumAgent 在深层网页（深度 > 6）中依然能保持较高的准确率，表现出极强的鲁棒性。

#### 7. 优点
*   **算法创新**：ATP-BFS 算法有效地平衡了搜索的深度与广度，解决了开放网络搜索中的“分支因子”过大问题。
*   **结构感知**：Cache Manager 的设计巧妙地利用了数据在 Web 站点中往往按行或按列对齐的物理特性。
*   **基准真实性**：SodiumBench 来源于真实的学术论文，具有极高的生态效度和实际应用价值。

#### 8. 不足与局限
*   **模型依赖**：高度依赖于顶尖的闭源模型（如 GPT-5），在较低阶模型上的泛化能力尚未验证。
*   **成本问题**：尽管通过缓存降低了成本，但对于大规模数据库的自动化构建，单单元格 0.41 美元的成本在商业大规模应用中可能依然偏高。
*   **极端深度挑战**：在极深层（深度 $\ge$ 6）且结构发散的网站中，路径复用的有效性会有所下降。

（完）
