---
title: "ResearchPilot: A Local-First Multi-Agent System for Literature Synthesis and Related Work Drafting"
title_zh: ResearchPilot：一种用于文献综述与相关工作撰写的本地优先多智能体系统
authors: Peng Zhang
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14629v1"
tags: ["query:sr"]
score: 10.0
evidence: 用于文献综述和相关工作起草的多智能体系统
tldr: ResearchPilot 是一个开源、可自托管的多智能体系统，旨在辅助文献综述。它通过自然语言研究问题，从 Semantic Scholar 和 arXiv 检索论文，提取摘要中的结构化发现，合成跨论文模式，并撰写带有引用的相关工作章节。该系统采用本地优先架构，结合了 FastAPI、Next.js 和 DSPy 等技术，强调透明度和用户自主性，是文献综述自动化领域的一个重要系统性贡献。
selection_source: fresh_fetch
motivation: 旨在解决研究人员在文献综述过程中面临的繁琐检索、信息合成和相关工作撰写挑战。
method: 开发了一个基于本地优先架构的多智能体系统，利用 DSPy 进行任务编排，并结合向量数据库和结构化提取技术。
result: 成功实现了从论文检索到结构化发现提取，再到自动生成带有引用的相关工作草稿的完整流程。
conclusion: ResearchPilot 作为一个系统性贡献，为透明且可自托管的科研辅助工具提供了参考，尽管在 API 限制和全文覆盖方面仍有改进空间。
---

## 摘要
ResearchPilot 是一个开源、可自托管的多智能体系统，旨在辅助文献综述工作。给定一个自然语言研究问题，该系统能从 Semantic Scholar 和 arXiv 检索论文，从论文摘要中提取结构化发现，合成跨论文模式，并撰写具有引用意识的相关工作章节。该系统采用本地优先架构，集成了 FastAPI、Next.js、DSPy、SQLite 和 Qdrant，支持“自带密钥”（BYOK）模型访问以及远程或本地嵌入。本文介绍了系统设计、类型化智能体接口、持久化与历史搜索机制，以及构建透明研究助手过程中的工程权衡。与其声称算法创新，我们更倾向于将 ResearchPilot 视为一项系统性贡献，并通过自动化测试和端到端本地运行对其进行了评估。最后，我们讨论了该系统的局限性，包括外部 API 速率限制、仅限于摘要的提取、语料库覆盖不全以及缺乏引用验证。

## Abstract
ResearchPilot is an open-source, self-hostable multi-agent system for literature-review assistance. Given a natural-language research question, it retrieves papers from Semantic Scholar and arXiv, extracts structured findings from paper abstracts, synthesizes cross-paper patterns, and drafts a citation-aware related-work section. The system combines FastAPI, Next.js, DSPy, SQLite, and Qdrant in a local-first architecture that supports bring-your-own-key model access and remote-or-local embeddings. This paper describes the system design, typed agent interfaces, persistence and history-search mechanisms, and the engineering tradeoffs involved in building a transparent research assistant. Rather than claiming algorithmic novelty, we present ResearchPilot as a systems contribution and evaluate it through automated tests and end-to-end local runs. We discuss limitations including external API rate limits, abstract-only extraction, incomplete corpus coverage, and the lack of citation verification.

---

## 论文详细总结（自动生成）

以下是对论文《ResearchPilot: A Local-First Multi-Agent System for Literature Synthesis and Related Work Drafting》的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：文献综述是科研中最耗时的阶段之一，涉及检索、阅读、笔记和撰写等多个环节。现有的 LLM 工具通常采用“一键式”生成，存在检索与推理模糊、中间证据不可见、来源难以追溯等“黑盒”问题。
*   **研究动机**：开发一个透明、可检查且本地优先（Local-first）的系统，将复杂的文献综述任务分解为结构化的多阶段流程，帮助研究人员从研究问题快速过渡到带有引用的相关工作草稿。

### 2. 核心方法论
ResearchPilot 采用四阶段多智能体流水线架构，利用 **DSPy** 框架进行任务编排和类型化接口管理：
*   **SearchAgent（检索智能体）**：异步查询 Semantic Scholar 和 arXiv，进行去重和过滤（仅保留有摘要的论文），支持部分失败容错。
*   **ExtractionAgent（提取智能体）**：针对每篇论文摘要，提取结构化的 JSON 数据，包含：主张（Claims）、方法、数据集、结果和局限性。
*   **SynthesisAgent（合成智能体）**：对比多篇论文的提取结果，识别共识（Consensus）、矛盾（Contradictions）和研究空白（Open Gaps）。
*   **WriterAgent（撰写智能体）**：基于合成结果和参考文献列表，生成带有内联引用标签（如 [R1]）的 Markdown 格式相关工作草稿。
*   **技术栈**：后端使用 FastAPI，前端使用 Next.js，持久化采用 SQLite（存储报告）和 Qdrant（向量存储，支持语义历史搜索）。支持本地（BAAI/bge-small-en-v1.5）或远程嵌入模型。

### 3. 实验设计
*   **定位**：本文定位为“系统性贡献”（Systems Contribution），而非算法创新，因此未进行大规模基准测试对比。
*   **测试场景**：通过具体的科研问题进行端到端验证，例如“RAG 在问答中的最新趋势”、“提高长文本 LLM 生成事实性的方法”等。
*   **对比对象**：未与 SOTA 模型进行指标对比，主要通过自动化功能测试和手动端到端运行来评估系统的正确性和鲁棒性。

### 4. 资源与算力
*   **算力说明**：论文未提及具体的 GPU 训练资源，因为该系统主要基于现有的预训练模型进行推理。
*   **运行环境**：实验在本地环境下运行，使用了 Groq 托管的 `llama-3.3-70b-versatile` 模型进行文本生成，并使用本地 CPU/GPU 运行 `bge-small-en-v1.5` 嵌入模型。
*   **部署方式**：系统通过 Docker Compose 打包，支持用户在个人机器上自托管。

### 5. 实验数量与充分性
*   **自动化测试**：包含 11 个后端测试和 1 个前端烟雾测试，验证了流式传输、报告持久化、去重逻辑和错误处理。
*   **手动验证**：记录了 2026 年 3 月 14 日的多次运行结果。例如，针对 RAG 问题的运行在 12.47 秒内完成，处理了 10 篇论文。
*   **充分性评价**：作为原型系统，其实验足以证明工作流的可行性和系统稳定性，但缺乏大规模的人类评估（如查全率、查准率或引用准确性的定量评分）。

### 6. 主要结论与发现
*   **流程可行性**：证明了将文献综述分解为“检索-提取-合成-撰写”四个显式阶段是可行的，且比单次提示词（One-shot）更具可解释性。
*   **外部依赖风险**：系统的可靠性高度受限于外部 API（如 Semantic Scholar 的 429 速率限制）和模型供应商的配额。
*   **本地优先优势**：通过本地嵌入和 SQLite 存储，系统在无远程嵌入 API 的情况下仍能保持核心功能（如历史搜索）。

### 7. 优点
*   **透明度高**：每个阶段的中间产物（如结构化提取结果）均可被用户检查，避免了 AI 幻觉的隐蔽性。
*   **模块化设计**：基于 DSPy 的签名设计使得更换模型供应商（OpenAI, Anthropic, Groq 等）非常简便。
*   **隐私与自主性**：本地优先架构和“自带密钥”模式保护了用户的研究意图和数据。
*   **鲁棒性**：具备处理 API 部分失效的容错机制和本地 fallback 路径。

### 8. 不足与局限
*   **数据源限制**：目前仅支持 Semantic Scholar 和 arXiv，且仅能处理摘要（Abstract-only），可能遗漏全文中的关键细节。
*   **引用验证缺失**：系统虽然能生成引用标签，但无法百分之百保证生成的句子与引用的原文内容完全对应（缺乏独立的引用核查智能体）。
*   **速率限制**：频繁调用 LLM 和外部检索 API 容易触发速率限制，影响连续使用体验。
*   **评估深度**：缺乏与人类专家撰写结果的对比实验，也未量化合成阶段的准确性。

（完）
