---
title: "Public Profile Matters: A Scalable Integrated Approach to Recommend Citations in the Wild"
title_zh: 公共概况至关重要：一种在真实场景下推荐引文的可扩展集成方法
authors: "Karan Goyal, Dikshant Kukreja, Vikram Goyal, Mukesh Mohania"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17361v1"
tags: ["query:sr"]
score: 9.0
evidence: 可扩展的集成引文推荐方法
tldr: 针对现有引文推荐系统忽视人类引用行为且计算成本高、存在偏差的问题，本文提出了一种轻量级、非学习型的模块 Profiler，用于高效捕捉引用模式。同时，针对现有评估协议无法反映真实场景的缺陷，引入了严格的归纳式评估设置。此外，开发了 DAVINCI 重排序模型，通过自适应向量门控机制融合先验知识与语义信息。该系统在多个基准测试中达到 SOTA，显著提升了引文推荐的效率与泛化能力。
selection_source: fresh_fetch
motivation: 现有的引文推荐系统往往忽略了人类引用行为的细微差别，且现有评估方法无法模拟真实世界中为新论文推荐引文的场景。
method: 提出了轻量级模块 Profiler 捕捉引用模式，并结合 DAVINCI 重排序模型，利用自适应向量门控机制整合置信度先验与语义信息。
result: 该方法在多个基准数据集上刷新了最先进的性能记录，并展现出极高的计算效率。
conclusion: 通过整合人类引用模式并采用更严格的归纳式评估，本研究为构建更实用、高效的引文推荐系统提供了新方向。
---

## 摘要
准确引用相关文献对于背景化和验证科学贡献至关重要。虽然目前的引文推荐系统利用了局部和全局文本信息，但它们往往忽略了人类引用行为的细微差别。最近一些结合此类模式的方法虽然提高了性能，但带来了高昂的计算成本，并为下游重排序器引入了系统性偏差。为解决这一问题，我们提出了 Profiler，这是一个轻量级、非学习型的模块，能够高效且无偏差地捕捉人类引用模式，从而显著增强候选检索。此外，我们发现了当前评估协议的一个关键局限：系统是在转导（transductive）设置下进行评估的，这无法反映真实世界的场景。我们引入了一种严格的归纳（inductive）评估设置，该设置强制执行严格的时间约束，模拟了在真实场景下为新撰写的论文推荐引文的过程。最后，我们提出了 DAVINCI，这是一种新型重排序模型，它通过自适应向量门控机制将源自 Profiler 的置信度先验与语义信息相结合。我们的系统在多个基准数据集上取得了新的最先进结果，展示了卓越的效率和泛化能力。

## Abstract
Proper citation of relevant literature is essential for contextualising and validating scientific contributions. While current citation recommendation systems leverage local and global textual information, they often overlook the nuances of the human citation behaviour. Recent methods that incorporate such patterns improve performance but incur high computational costs and introduce systematic biases into downstream rerankers. To address this, we propose Profiler, a lightweight, non-learnable module that captures human citation patterns efficiently and without bias, significantly enhancing candidate retrieval. Furthermore, we identify a critical limitation in current evaluation protocol: the systems are assessed in a transductive setting, which fails to reflect real-world scenarios. We introduce a rigorous Inductive evaluation setting that enforces strict temporal constraints, simulating the recommendation of citations for newly authored papers in the wild. Finally, we present DAVINCI, a novel reranking model that integrates profiler-derived confidence priors with semantic information via an adaptive vector-gating mechanism. Our system achieves new state-of-the-art results across multiple benchmark datasets, demonstrating superior efficiency and generalisability.

---

## 论文详细总结（自动生成）

这篇论文提出了一种名为 **DAVINCI** 的新型引文推荐框架，旨在解决局部引文推荐（LCR）在真实场景（In the Wild）应用中的效率、偏差及评估协议不真实的问题。以下是对该论文的深度结构化总结：

### 1. 核心问题与整体含义
*   **研究动机**：随着科研文献爆炸式增长，自动引文推荐变得至关重要。然而，现有系统存在三大缺陷：
    1.  **忽视人类行为**：现有模型往往只关注文本语义，忽略了论文在学术网络中的“公共形象”（即它是如何被他人引用的）。
    2.  **计算成本高且有偏差**：引入引用模式的现有方法（如 SymTax）通常计算开销巨大，且容易引入“确认偏差”。
    3.  **评估协议不切实际**：大多数研究采用“转导式（Transductive）”评估，即候选库包含测试集中的论文，这在现实中（为正在写的论文找引文）是不可能的。
*   **核心目标**：建立一个可扩展、无偏差、且符合现实时间逻辑（归纳式设置）的集成引文推荐系统。

### 2. 论文提出的方法论
该系统分为两个阶段：**Profiler（检索）** 和 **DAVINCI（重排序）**。

*   **Profiler（非学习型检索模块）**：
    *   **核心思想**：通过“公共概况（Public Profile）”增强论文表示。论文的身份不仅取决于其自身内容，还取决于引用它的上下文。
    *   **技术细节**：这是一个静态富化过程。对于库中每篇论文 $D_i$，其最终向量 $\hat{v}_i$ 是其原始向量 $v_i$ 与引用它的论文向量 $v_j$ 及引用上下文向量 $v_{sji}$ 的加权平均。
    *   **公式**：$\hat{v}_i = v_i + \frac{1}{|N_{in}(D_i)|} \sum ( \alpha \cdot v_{sji} + \beta \cdot v_j )$。这种设计解决了冷启动问题（无引用的新论文退化为原始向量），且无需训练，极其高效。
*   **DAVINCI（自适应重排序模型）**：
    *   **判别式先验**：将 Profiler 输出的原始得分转换为排名，再通过指数衰减函数 $p_i = \lambda^{r_i}$ 映射为连续的置信度先验，以捕捉相关性的幂律分布。
    *   **自适应向量门控机制**：使用门控网络（Gating Network）生成一个向量 $g$，对语义特征和得分特征进行逐维度的“软掩码（Soft Masking）”融合。
    *   **公式**：$h_{fused} = g \odot [h_{text}; h_{score}]$。这种细粒度的融合比简单的标量加权更具表达力。

### 3. 实验设计
*   **数据集**：使用了五个主流基准数据集：ACL-200、Full-TextPeerRead (FTPR)、RefSeer、arXiv 和 ArSyTa（规模从几千到八百万个引文上下文不等）。
*   **归纳式评估设置（Inductive Setting）**：这是本文的重要贡献。强制执行时间约束：候选库只能包含在查询论文发表日期之前的文献，且查询论文不在候选库中。
*   **对比方法**：
    *   传统方法：BM25。
    *   SOTA 专用模型：HAtten, SymTax, SciNCL。
    *   **大规模重排序器**：Qwen3-Reranker-8B (8.1B 参数) 和 bge-reranker-v2-minicpm-40 (2.7B 参数)。

### 4. 资源与算力
*   **硬件使用**：
    *   **检索阶段**：在 NVIDIA A100 DGX 集群上执行。
    *   **重排序阶段**：在 NVIDIA H200 DGX 系统上执行。
*   **训练细节**：论文未明确给出总训练时长，但提到为了验证稳定性，在代表性子集上进行了三次试验，观察到极小的性能波动，最终结果基于全量数据的单次运行。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了从小型到超大型（800万条数据）的五个数据集，实验规模非常充分。
*   **消融实验**：设计了四组消融实验（A1-A4），分别验证了置信度先验、判别器转换、归一化方式以及向量门控机制的有效性。
*   **公平性**：通过指令感知提示（Instruction-aware prompting）将大规模模型（如 Qwen3）适配到相同任务下进行对比，确保了跨规模比较的客观性。

### 6. 主要结论与发现
*   **性能领先**：DAVINCI 在所有数据集和所有指标（MRR, Recall@K, NDCG@K）上均刷新了 SOTA 记录。
*   **效率惊人**：Profiler 模块在最大数据集上的检索速度比之前的 SOTA（SymTax）快了 **32.52 倍**，在小数据集上快了 **43.92 倍**。
*   **以小博大**：仅有 **1.1亿（110M）** 参数的 DAVINCI 模型显著优于参数量大其 70 倍的 **81亿（8B）** 参数通用重排序模型。
*   **公共形象的重要性**：实验证明，一旦关闭 Profiler 中的公共概况富化功能，检索性能会发生剧烈崩溃。

### 7. 优点
*   **现实意义强**：提出的“归纳式评估”更符合科研人员真实的写作场景。
*   **架构优雅**：Profiler 采用非学习型设计，极大地降低了系统维护和更新的计算成本。
*   **参数高效**：证明了在特定领域（如学术引文），任务对齐的架构设计比单纯堆砌参数量更有效。

### 8. 不足与局限
*   **领域局限**：实验主要集中在计算机科学（CS）领域的英文文献，尚未验证在人文社科或其他语言中的表现。
*   **冷启动挑战**：虽然有退化机制，但对于完全没有任何被引记录的极新论文，Profiler 的优势无法发挥，仍依赖纯文本语义。
*   **输入长度限制**：受限于 Transformer 的 512 长度限制，对于需要超长上下文理解的引文推荐可能存在截断风险。
*   **元数据依赖**：系统性能高度依赖于标题、摘要等元数据的质量，若元数据缺失或解析错误，效果会打折扣。

（完）
