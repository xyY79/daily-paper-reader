---
title: "CRE-T1 Preview Technical Report: Beyond Contrastive Learning for Reasoning-Intensive Retrieval"
title_zh: CRE-T1 预览技术报告：超越对比学习，实现推理密集型检索
authors: "Guangzhi Wang, Yinghao Jiao, Zhi Liu"
date: 2026-03-18
pdf: "https://arxiv.org/pdf/2603.17387v1"
tags: ["query:sr"]
score: 9.0
evidence: 用于识别隐含关系的推理密集型检索
tldr: 针对推理密集型检索中对比学习无法处理隐式逻辑关系的局限，本文提出生成式检索模型T1。该模型将静态对齐转变为动态推理，通过为查询生成中间推理轨迹并利用特定标记聚合语义，结合三阶段训练及GRPO强化学习优化推理策略。实验表明，T1-4B在BRIGHT基准上表现优异，证明了动态推理在提升复杂检索任务性能方面的有效性。
selection_source: fresh_fetch
motivation: 传统的对比学习范式属于静态表示整合，难以处理查询与文档之间复杂的隐式推理关系及词汇不匹配问题。
method: 提出T1模型，通过生成中间推理轨迹实现动态建模，并引入GRPO强化学习算法优化模型的推导策略。
result: T1-4B在BRIGHT基准测试中超越了更大规模的对比学习模型，达到了与多阶段检索流水线相当的性能。
conclusion: 将静态表示对齐替换为动态推理生成，能显著提升推理密集型检索任务的性能。
---

## 摘要
推理密集型检索的核心挑战在于识别查询与文档之间隐含的推理关系，而非表层的语义或词汇相似性。对比学习范式本质上是一种静态表示巩固技术：在训练期间，它将层级相关性概念编码为向量空间中固定的几何结构；在推理阶段，它无法根据每个查询的具体推理需求动态调整相关性判断。因此，当查询与文档之间存在词汇不匹配，或需要隐含推理来建立相关性时，性能会显著下降。本文提出了 Thought 1 (T1)，这是一种生成式检索模型，将相关性建模从静态对齐转变为动态推理。在查询端，T1 为每个查询动态生成中间推理轨迹，以桥接隐含的推理关系，并使用 <embtoken> 作为推理输出的语义聚合点。在文档端，它采用“指令 + 文本 + <embtoken>”的编码格式，以支持高吞吐量索引。为了将动态推理能力内化到向量表示中，我们采用了三阶段训练课程，并在第三阶段引入了 GRPO，使模型能够通过试错强化学习为不同查询学习最优推导策略。在 BRIGHT 基准测试中，T1-4B 在原始查询设置下表现出强劲性能，整体优于使用对比学习训练的更大规模模型，并达到了与多阶段检索流水线相当的性能。结果表明，用动态推理生成取代静态表示对齐可以有效提升推理密集型检索的性能。

## Abstract
The central challenge of reasoning-intensive retrieval lies in identifying implicitreasoning relationships between queries and documents, rather than superficial se-mantic or lexical similarity. The contrastive learning paradigm is fundamentallya static representation consolidation technique: during training, it encodes hier-archical relevance concepts into fixed geometric structures in the vector space,and at inference time it cannot dynamically adjust relevance judgments accord-ing to the specific reasoning demands of each query. Consequently, performancedegrades noticeably when vocabulary mismatch exists between queries and doc-uments or when implicit reasoning is required to establish relevance. This pa-per proposes Thought 1 (T1), a generative retrieval model that shifts relevancemodeling from static alignment to dynamic reasoning. On the query side, T1 dy-namically generates intermediate reasoning trajectories for each query to bridgeimplicit reasoning relationships and uses <embtoken> as a semantic aggregationpoint for the reasoning output. On the document side, it employs an instruction+ text + <embtoken> encoding format to support high-throughput indexing. Tointernalize dynamic reasoning capabilities into vector representations, we adopt athree-stage training curriculum and introduce GRPO in the third stage, enablingthe model to learn optimal derivation strategies for different queries through trial-and-error reinforcement learning. On the BRIGHT benchmark, T1-4B exhibitsstrong performance under the original query setting, outperforming larger modelstrained with contrastive learning overall, and achieving performance comparableto multi-stage retrieval pipelines. The results demonstrate that replacing static rep-resentation alignment with dynamic reasoning generation can effectively improvereasoning-intensive retrieval performance.

---

## 论文详细总结（自动生成）

### CRE-T1 预览技术报告：超越对比学习，实现推理密集型检索

#### 1. 核心问题与整体含义
论文针对**推理密集型检索（Reasoning-Intensive Retrieval）**中的核心瓶颈提出挑战。传统的对比学习（Contrastive Learning）范式属于“静态表示整合”，它将相关性预先编码为向量空间中固定的几何结构。这种方式在面对词汇不匹配或需要隐式逻辑（如因果、多跳推理）的复杂查询时表现不佳，因为模型在推理阶段无法根据具体查询动态调整判断策略。论文提出的 **T1 (Thought 1)** 模型旨在将检索从“静态对齐”转变为“动态推理”，使模型在生成向量前先进行显式的思维链推导。

#### 2. 方法论
T1 模型基于 **Qwen3-Instruct-4B** 构建，核心思想是**“先推理，后表征”**。
*   **非对称架构设计**：
    *   **查询端（Query Side）**：模型接收查询后，先自回归生成一段中间推理轨迹（Reasoning Trajectory），最后输出一个特殊的 `<emb token>`。取该 token 最后一层的隐藏状态作为查询向量 $e_q$。
    *   **文档端（Doc Side）**：为了保持高吞吐索引效率，文档端不生成推理，而是采用“指令 + 文档内容 + `<emb token>`”的格式进行单次前向编码，提取向量 $e_d$。
*   **三阶段训练课程**：
    *   **阶段 1：任务感知（Task Awareness）**。使用 MS MARCO 数据集进行冷启动，确立输出格式并初步建立检索判别能力。
    *   **阶段 2：推理对齐（Reasoning Alignment）**。引入合成的推理数据（ReasonEmbed），通过 SFT 让模型学会生成简洁的推理路径。
    *   **阶段 3：推理优化（Reasoning Optimization）**。引入 **GRPO（群体相对策略优化）** 强化学习。通过采样多个推理分支，利用可微排名奖励（$R_{rank}$）和格式奖励（$R_{format}$）进行试错学习，优化模型在不同查询下的推导策略。

#### 3. 实验设计
*   **基准测试**：主要使用 **BRIGHT** 基准，这是目前针对推理密集型检索最权威的测试集，涵盖数学（AoPS, LeetCode）、科学（Bio, Earth）、法律、常识等 12 个子领域。
*   **对比方法**：
    *   **检索模型**：ReasonIR-8B、RaDeR-7B、Seed1.5-Embedding。
    *   **流水线组合**：对比了“检索 + GPT-4 查询改写”以及“检索 + QwenRerank 重排序”的复杂流水线。
    *   **消融对比**：对比了同规模（4B）下使用纯对比学习训练的模型。
*   **评价指标**：nDCG@10。

#### 4. 资源与算力
论文明确记录了实验的算力消耗：
*   **硬件**：4 台 **NVIDIA A800 GPU**。
*   **训练时长**：
    *   阶段 1：20 小时。
    *   阶段 2：8 小时。
    *   阶段 3：20 小时。
*   **总计**：约 48 小时完成 4B 规模模型的全流程训练。

#### 5. 实验数量与充分性
*   **实验规模**：在 BRIGHT 的 12 个子任务上进行了全面评估，并进行了三阶段训练的消融实验（Stage 1 vs Stage 2 vs Stage 3）。
*   **充分性**：实验设计较为充分，不仅对比了单模型性能，还跨量级对比了 7B/8B 模型，并挑战了“检索+重排”的工业级流水线。
*   **客观性**：通过在原始查询（Original Query）下直接对比，证明了 T1 无需外部改写工具即可内化推理能力，对比过程公平。

#### 6. 主要结论与发现
*   **性能卓越**：T1-4B 在 BRIGHT 上的平均 nDCG@10 达到 37.1，显著超过了 ReasonIR-8B (24.4) 和 Seed1.5 (27.2)。
*   **单模型胜过流水线**：T1-4B 单模型的表现甚至优于“传统检索 + GPT-4 改写”的组合，并达到了与“检索 + 重排序”流水线持平的效果。
*   **动态推理的价值**：实验证明，通过强化学习内化推理轨迹，比单纯增加对比学习的负采样或数据量更能有效解决隐式相关性问题。

#### 7. 优点
*   **范式创新**：成功将 LLM 的思维链（CoT）能力与向量检索融合，打破了对比学习的静态限制。
*   **效率平衡**：通过非对称设计，在查询端投入推理计算，而在文档端保持高效编码，兼顾了检索精度与索引效率。
*   **强化学习应用**：首次展示了 GRPO 在优化检索向量表示方面的潜力，通过排名奖励直接优化推理质量。

#### 8. 不足与局限
*   **领域差异性**：在符号性极强或格式极其敏感的领域（如 LeetCode 编程、AoPS 数学竞赛），T1 的提升有限甚至出现轻微退化，说明通用推理奖励可能无法覆盖所有专业领域。
*   **奖励噪声**：强化学习在处理分布外（OOD）领域时，可能受到奖励估计噪声的影响，导致过度优化或轻微遗忘。
*   **推理开销**：虽然避免了重排阶段的巨大开销，但查询端生成推理序列仍会带来一定的首字延迟（Latency）。

（完）
