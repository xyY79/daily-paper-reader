---
title: "ResearchPilot: A Local-First Multi-Agent System for Literature Synthesis and Related Work Drafting"
title_zh: ResearchPilot：一种用于文献综述与相关工作起草的本地优先多智能体系统
authors: Peng Zhang
date: 2026-03-15
pdf: "https://arxiv.org/pdf/2603.14629v1"
tags: ["query:sr"]
score: 10.0
evidence: 用于文献综述和相关工作起草的多智能体系统
tldr: ResearchPilot 是一个开源、可自托管的多智能体系统，旨在辅助文献综述。它通过自然语言研究问题，从 Semantic Scholar 和 arXiv 检索论文，提取摘要中的结构化发现，合成跨论文模式，并撰写带有引用的相关工作章节。该系统采用本地优先架构，结合了 FastAPI、Next.js 和 DSPy 等技术，强调透明度和用户自主性，是文献综述自动化领域的一个重要系统性贡献。
selection_source: fresh_fetch
motivation: 旨在解决研究人员在文献综述过程中面临的论文检索、信息提取和相关工作撰写效率低下的问题。
method: 开发了一个基于本地优先架构的多智能体系统，利用 DSPy 进行任务编排，并结合向量数据库和结构化存储实现文献合成。
result: 成功实现了从检索到草拟相关工作章节的自动化流程，并通过自动化测试和端到端运行验证了系统的可行性。
conclusion: ResearchPilot 提供了一个透明且可扩展的文献综述辅助工具，尽管在 API 限制和全文覆盖方面存在局限，但为自动化科研协作提供了系统参考。
---

## 摘要
ResearchPilot 是一个开源、可自托管的多智能体系统，用于辅助文献综述。给定一个自然语言研究问题，该系统从 Semantic Scholar 和 arXiv 检索论文，从论文摘要中提取结构化发现，综合跨论文模式，并起草一个具备引用意识的相关工作章节。该系统在本地优先架构中结合了 FastAPI、Next.js、DSPy、SQLite 和 Qdrant，支持自带密钥（BYOK）的模型访问以及远程或本地嵌入。本文描述了系统设计、类型化智能体接口、持久化与历史搜索机制，以及构建透明研究助手所涉及的工程权衡。我们并非主张算法创新，而是将 ResearchPilot 作为一项系统性贡献提出，并通过自动化测试和端到端本地运行对其进行评估。我们讨论了该系统的局限性，包括外部 API 速率限制、仅限摘要的提取、语料库覆盖不完整以及缺乏引用验证。

## Abstract
ResearchPilot is an open-source, self-hostable multi-agent system for literature-review assistance. Given a natural-language research question, it retrieves papers from Semantic Scholar and arXiv, extracts structured findings from paper abstracts, synthesizes cross-paper patterns, and drafts a citation-aware related-work section. The system combines FastAPI, Next.js, DSPy, SQLite, and Qdrant in a local-first architecture that supports bring-your-own-key model access and remote-or-local embeddings. This paper describes the system design, typed agent interfaces, persistence and history-search mechanisms, and the engineering tradeoffs involved in building a transparent research assistant. Rather than claiming algorithmic novelty, we present ResearchPilot as a systems contribution and evaluate it through automated tests and end-to-end local runs. We discuss limitations including external API rate limits, abstract-only extraction, incomplete corpus coverage, and the lack of citation verification.

---

## 论文详细总结（自动生成）

### ResearchPilot：一种用于文献综述与相关工作起草的本地优先多智能体系统

#### 1. 论文的核心问题与整体含义
*   **研究动机**：文献综述是科研中最耗时的环节之一，涉及搜索、阅读、笔记和撰写等多个重复循环。虽然大语言模型（LLM）在摘要和提取方面表现出色，但传统的“单次提示词（one-shot prompting）”方法往往将检索与推理混为一谈，导致中间证据不可见，且难以验证生成内容的来源（Provenance）。
*   **核心目标**：开发一个名为 **ResearchPilot** 的开源、本地优先（Local-first）的多智能体系统。该系统旨在将文献综述任务分解为透明、可检查的四个阶段，帮助研究人员从自然语言问题快速生成结构化的相关工作草案。

#### 2. 论文提出的方法论
ResearchPilot 采用了一种固定的四阶段顺序流水线架构，利用 **DSPy** 框架进行智能体编排：
*   **SearchAgent（检索智能体）**：异步查询 Semantic Scholar 和 arXiv。具备容错机制，若某一来源失败（如触发频率限制），仍可利用另一来源的数据。输出包含摘要和元数据的论文列表。
*   **ExtractionAgent（提取智能体）**：针对每篇论文摘要，利用 DSPy 签名提取结构化 JSON 数据，涵盖：主张（Claims）、方法（Methods）、数据集（Datasets）、结果（Results）和局限性（Limitations）。
*   **SynthesisAgent（合成智能体）**：对比多篇论文的提取结果，识别共识（Consensus）、矛盾（Contradictions）和研究空白（Open Gaps）。
*   **WriterAgent（撰写智能体）**：基于合成结果和参考文献列表，生成带有行内引用标签（如 [R1]）的 Markdown 格式相关工作草案。
*   **技术栈**：后端使用 FastAPI 提供流式 API，前端使用 Next.js，持久化层结合 SQLite（存储报告）和 Qdrant（向量数据库，用于历史报告的语义搜索）。

#### 3. 实验设计
*   **实验性质**：本文定位为“系统性贡献（Systems Contribution）”，而非算法创新，因此未进行大规模模型基准测试。
*   **自动化验证**：包含 11 个后端测试和 1 个前端冒烟测试，验证流式传输、错误处理、去重逻辑、嵌入模式切换等功能。
*   **手动端到端验证**：通过具体的研究问题（如“RAG 在问答系统中的最新趋势”、“GNN 在分子属性预测中的局限性”）在本地环境中运行系统。
*   **对比对象**：未与同类商业工具进行量化对比，重点在于验证系统在真实 API 限制和模型响应下的鲁棒性。

#### 4. 资源与算力
*   **算力说明**：论文未详细列出具体的 GPU 型号或训练时长，因为该系统主要作为推理侧的编排框架。
*   **模型使用**：实验中使用了 Groq 托管的 `llama-3.3-70b-versatile` 模型。
*   **嵌入模型**：默认使用本地运行的开源模型 `BAAI/bge-small-en-v1.5`，以实现本地优先的特性。

#### 5. 实验数量与充分性
*   **实验规模**：进行了小规模的端到端运行测试（如 2026 年 3 月 14 日记录的一次成功运行和一次因速率限制失败的批处理测试）。
*   **充分性评价**：对于系统原型验证而言，实验涵盖了核心功能路径和常见的失效模式（如 API 429 错误）。但从学术严谨性角度看，缺乏人类评估（Human Evaluation）来衡量生成草案的质量，也缺乏大规模的消融实验。

#### 6. 论文的主要结论与发现
*   **透明度优势**：将任务分解为显式模块比单次提示词更易于检查、调试和扩展。
*   **外部约束**：系统的可靠性主要受限于外部 API 的速率限制（Rate Limits）和模型供应商的 Token 配额，而非算法本身。
*   **本地优先的可行性**：通过本地嵌入模型和向量数据库，可以在不依赖中心化服务的情况下实现高效的文献管理和历史搜索。

#### 7. 优点
*   **模块化设计**：使用 DSPy 定义类型化接口，使得更换模型供应商（OpenAI, Anthropic, Groq 等）变得非常简单。
*   **透明与可追溯**：用户可以查看每个阶段的中间产物（如每篇论文的结构化提取结果），降低了 AI 幻觉带来的风险。
*   **本地化与隐私**：支持自托管和自带密钥（BYOK），符合科研人员对数据隐私的需求。

#### 8. 不足与局限
*   **数据源局限**：目前仅支持 Semantic Scholar 和 arXiv，且仅能处理摘要（Abstract-only），无法深入分析论文全文中的实验细节。
*   **引用验证缺失**：系统虽然能生成引用标签，但无法独立验证生成文本是否百分之百由引用的论文支撑。
*   **评估深度不足**：缺乏与人类撰写草案的对比，也未在医学、工程等特定领域进行深度验证。
*   **性能瓶颈**：目前提取阶段是串行运行的，处理大量论文时效率较低。

（完）
